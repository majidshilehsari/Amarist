# راهنمای ایجنت بعدی (Agent Guide)

این فایل برای هوش مصنوعی‌های بعدی است که روی مخزن **آماریست (Amarist)** کار می‌کنند.
تجربه‌های تکمیلی را اینجا ثبت کنید تا ایجنت بعدی سریع‌تر مسیر را پیدا کند.

## ۱. محصول چیست؟
آماریست سرویس آنلاین **تحلیل آماری داده‌های پژوهشی** و **تولید داده‌های تمرینی هدفمند**
برای اساتید آمار و روش تحقیق است. بدون دیتابیس و بدون ثبت‌نام؛ ذخیره‌سازی در مرورگر
(localStorage). تکنولوژی: Next.js 16 (App Router)، TypeScript، Tailwind CSS v4، فونت وزیرمتن.

## ۲. وضعیت فعلی (تا نسخه ۱)
- فقط **لندینگ‌پیج** (فاز ۱ نقشه‌راه) پیاده‌سازی شده است.
- فاز ۲ (ورود داده و تحلیل)، فاز ۳ (تولید دادهٔ تمرینی) و فاز ۴ (خروجی اکسل/ورد/CSV) هنوز نیستند.
- چهار «حالت پژوهشی» در `src/components/research-modes.tsx` تعریف شده‌اند (در دست ساخت):
  اثربخشی یک درمان، مقایسه دو درمان، معادلات ساختاری (SEM)، پیش‌بینی رگرسیونی.

## ۳. مفهوم «پروژه‌های پیش‌فرض» (Default Projects)
- قرارداد داده در `src/data/default-projects/types.ts` تعریف شده است.
- هر پروژه پیش‌فرض = یک الگوی سناریوی پژوهشی قابل بارگذاری (جمعیت، روش نمونه‌گیری،
  متغیرهای مدل، پرسشنامه‌ها با مقیاس/نمره‌دهی/خرده‌مقیاس/پایایی).
- نخستین پروژه: **«تارا رضوانی»** (`tara-razvani.ts`) — طرح SEM درباره اعتیاد به بازی‌های
  آنلاین، تنظیم هیجان، پرخاشگری و احساس ناکامی در نوجوانان رشت.
- **چگونه پروژه جدید اضافه کنید:** فایل جدید در `src/data/default-projects/` بسازید و به آرایهٔ
  `defaultProjects` در `index.ts` اضافه کنید.
- این داده‌ها هنوز در هیچ رابط کاربری نمایش داده نمی‌شوند؛ مصرف‌کننده‌شان موتور فاز ۳ است.

## ۴. قوانین گیت‌هاب و شاخه (خیلی مهم)
- **هرگز** روی `main` یا `production` کار نکنید. فقط روی یک شاخهٔ preview (مثل `arena/...`).
- هر بار فقط **یک بار** push کنید و آن هم بعد از اتمام همهٔ تغییرات (قانون ۴.۶).
- پیام کامیت/push باید **تاریخ و زمان شمسی با ساعت تهران** داشته باشد (قانون ۴.۸).
- قبل از هر کاری، اتصال به GitHub را چک کنید (قانون ۴.۷).
- مسئولیت ادغام با main به عهدهٔ کاربر است؛ شما فقط در شاخهٔ preview پوش کنید.

## ۵. محافظت از دیتابیس/بک‌اند (قانون ۴.۴)
- در این پروژه دیتابیس/بک‌اندی وجود ندارد. اگر روزی چنین چیزی اضافه شد، هر تغییر در آن را
  ابتدا با کاربر بحث کنید (فرمت «وضعیت اضطراری» با عدد ۵ رقمی تأیید) و بی‌اجازه اعمال نکنید.

## ۶. نکات فنی
- `AGENTS.md` توسط خودِ Next.js مدیریت می‌شود (بلوک قوانین فریمورک)؛ آن را دستی تغییر ندهید.
  راهنمای مربوط به فریمورک را از `node_modules/next/dist/docs/` بخوانید.
- خطای از پیش موجود: `src/app/layout.tsx` به `LayoutProps` ارجاع دارد که در Next.js 16 احتمالاً
  حذف شده؛ برای اصلاح، مستندات نسخهٔ ۱۶ را بخوانید. این خطا خارج از محدودهٔ افزودن پروژهٔ
  پیش‌فرض بود و در این نسخه اصلاح نشد.

## ۷. آخرین وضعیت شاخه و کامیت
- شاخه: `arena/01a037d0-amarist`
- شماره کامیت نسخه ۱ (محتوای پروژهٔ پیش‌فرض): `____HASH____` — ۱۴۰۵/۰۶/۰۳ (تهران)
- جزئیات بیشتر در `VERSION_LOG.md`.
