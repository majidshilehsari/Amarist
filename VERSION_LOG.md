# فایل مدیریت نسخه‌ها (Version Management)

> این فایل تاریخچهٔ نسخه‌های کد را ثبت می‌کند تا ایجنت بعدی دقیقاً بداند آخرین وضعیت چیست.
> هر بار که تغییر مهمی انجام شد، یک ورودی جدید در بالای بخش «نسخه‌ها» اضافه کنید.

## آخرین نسخه (خلاصه سریع برای ایجنت بعدی)

- **آیا آخرین نسخه است؟** بله (در شاخهٔ preview فعلی)
- **شاخه:** `arena/01a037d0-amarist`
- **شماره کامیت نسخه ۱:** `____HASH____`
- **تاریخ/زمان کامیت (تهران، شمسی):** ۱۴۰۵/۰۶/۰۳ — ____TIME____ (تهران)
- **خلاصه:** ایجاد ساختار «پروژه‌های پیش‌فرض» و افزودن نخستین پروژه «تارا رضوانی».

## نسخه‌ها

### نسخه ۱ — ایجاد پروژه‌های پیش‌فرض + پروژه «تارا رضوانی»

1. **تاریخ و زمان کامیت (تهران، شمسی):** ۱۴۰۵/۰۶/۰۳ — ____TIME____ (تهران)
2. **آیا این آخرین نسخه است:** بله.
3. **توضیح مختصر تغییرات نسبت به قبلی:**
   - نخستین نسخهٔ این فایل مدیریت نسخه‌هاست (قبلاً وجود نداشت).
   - ایجاد قرارداد داده‌ای پروژه‌های پیش‌فرض در `src/data/default-projects/types.ts`.
   - افزودن نخستین پروژه پیش‌فرض «تارا رضوانی» (`src/data/default-projects/tara-razvani.ts`) شامل ۴ پرسشنامه: OGAI، ERQ، AQ، مقیاس احساس ناکامی + پرسشنامه جمعیت‌شناختی.
   - ثبت فهرست پروژه‌ها در `src/data/default-projects/index.ts`.
   - ایجاد `AGENT_GUIDE.md` (راهنمای ایجنت بعدی).
4. **شاخه:** `arena/01a037d0-amarist`
5. **شماره کامیت:** `____HASH____`
6. **اطلاعات اضافی برای ایجنت بعدی:**
   - هشدار: در `src/app/layout.tsx` یک خطای تایپ از پیش موجود هست (`LayoutProps` تعریف نشده — احتمالاً تغییرِ شکستن‌دهنده در Next.js 16). این خطا ربطی به کار این نسخه ندارد و از قبل در مخزن بود؛ برای رفع، مستندات `node_modules/next/dist/docs/` را بخوانید.
   - «پروژه‌های پیش‌فرض» فعلاً فقط داده (static) هستند و در هیچ رابط کاربری نمایش داده نمی‌شوند؛ مصرف‌کنندهٔ بعدی آن‌ها موتور تولید دادهٔ تمرینی (فاز ۳ نقشه‌راه) خواهد بود.
   - این فایل در یک کامیت تکمیلی (ثبت شمارهٔ کامیت نسخه ۱) به‌روزرسانی شد؛ برای بازیابی دقیق وضعیت نسخه ۱، کامیت `____HASH____` را چک‌اوت کنید.
   - ترتیب گویه‌ها در خرده‌مقیاس‌های OGAI و AQ بر اساس ساختار متداول منتشرشده درج شده و در یادداشت (`note`) هر خرده‌مقیاس علامت‌گذاری شده که با متن دقیق نسخه فارسی بازبینی شود.
   - هیچ دیتابیس یا بک‌اندی در پروژه وجود ندارد (ذخیره‌سازی در localStorage). طبق قانون ۴.۴ کاربر، هرگونه تغییر در دیتابیس/بک‌اند باید ابتدا با کاربر بحث و تأیید شود.
