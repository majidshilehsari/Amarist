# فایل مدیریت نسخه‌ها (Version Management)

> این فایل تاریخچهٔ نسخه‌های کد را ثبت می‌کند تا ایجنت بعدی دقیقاً بداند آخرین وضعیت چیست.
> هر بار که تغییر مهمی انجام شد، یک ورودی جدید در بالای بخش «نسخه‌ها» اضافه کنید.

## آخرین نسخه (خلاصه سریع برای ایجنت بعدی)

- **آیا آخرین نسخه است؟** بله (در شاخهٔ preview فعلی)
- **شاخه:** `arena/01a04632-amarist`
- **شماره کامیت نسخه ۲:** `__COMMIT_HASH_V2__`
- **تاریخ/زمان کامیت (تهران، شمسی):** ۱۴۰۵/۰۶/۰۶ (تهران)
- **خلاصه:** افزودن پروژهٔ پیش‌فرض «تارا رضوانی» به ابزار SEM (در کنار «شیردل») + ادغام زیرساخت پروژه‌های پیش‌فرض استاتیک و فایل‌های راهنما/نسخه.

## نسخه‌ها

### نسخه ۲ — پروژهٔ پیش‌فرض «تارا رضوانی» در ابزار SEM + ادغام دو خط کاری

1. **تاریخ و زمان کامیت (تهران، شمسی):** ۱۴۰۵/۰۶/۰۶ (تهران)
2. **آیا این آخرین نسخه است:** بله.
3. **توضیح مختصر تغییرات نسبت به قبلی:**
   - پایهٔ این نسخه، آخرین کامیت ابزار SEM است: `8bf7abe` از شاخهٔ `arena/01a00a94-amarist`
     (کاربر آن را «آخرین و بی‌نقص» معرفی کرد).
   - افزودن `taraRazvaniVars` و منطق seeding در `src/app/tools/sem/page.tsx`: پروژهٔ پیش‌فرض
     «پروژه پیش‌فرض (تارا رضوانی)» با ۴ متغیر SEM (اعتیاد به بازی‌های آنلاین = برون‌زا،
     تنظیم هیجان = برون‌زا بدون نمرهٔ کل، احساس ناکامی = میانجی، پرخاشگری = درون‌زا) و
     دامنه‌های دقیقِ تأییدشده از متن فصل سوم.
   - بازآرایی seeding به دو تابع `ensureShirdelProject` و `ensureTaraRazvaniProject`
     (ادغام‌پذیر و غیرمخرب برای پروژه‌های موجودِ کاربر).
   - ادغام فایل‌های شاخهٔ `arena/01a037d0-amarist`: `src/data/default-projects/*` (متادیتای
     استاتیک پرسشنامه‌ها) + `AGENT_GUIDE.md` + `VERSION_LOG.md`.
4. **شاخه:** `arena/01a04632-amarist`
5. **شماره کامیت:** `__COMMIT_HASH_V2__`
6. **اطلاعات اضافی برای ایجنت بعدی:**
   - خطای تایپ `LayoutProps` در `src/app/layout.tsx` همچنان باز است (پیش‌موجود؛ `next dev`
     کار می‌کند ولی `npx tsc --noEmit` و `npm run build` خطا می‌دهند). خارج از محدودهٔ این نسخه بود.
   - ترتیب seed شدن پروژه‌ها در localStorage: «پروژه پیش‌فرض» → «شیردل» → «تارا رضوانی».
   - دو مفهوم «پروژهٔ پیش‌فرض» وجود دارد؛ توضیح کامل در `AGENT_GUIDE.md` بخش ۳.
   - هیچ دیتابیس یا بک‌اندی تغییر نکرده است (ذخیره‌سازی فقط در localStorage مرورگر).

### نسخه ۱ — ایجاد پروژه‌های پیش‌فرض (استاتیک) + پروژه «تارا رضوانی»

1. **تاریخ و زمان کامیت (تهران، شمسی):** ۱۴۰۵/۰۶/۰۳ — 11:06 (تهران)
2. **آیا این آخرین نسخه است:** خیر (نسخهٔ ۲ آخرین است).
3. **توضیح مختصر تغییرات نسبت به قبلی:**
   - نخستین نسخهٔ این فایل مدیریت نسخه‌ها بود (قبلاً وجود نداشت).
   - ایجاد قرارداد داده‌ای پروژه‌های پیش‌فرض در `src/data/default-projects/types.ts`.
   - افزودن نخستین پروژه پیش‌فرض استاتیک «تارا رضوانی» (`src/data/default-projects/tara-razvani.ts`)
     شامل ۴ پرسشنامه: OGAI، ERQ، AQ، مقیاس احساس ناکامی + پرسشنامه جمعیت‌شناختی.
   - ثبت فهرست پروژه‌ها در `src/data/default-projects/index.ts`.
   - ایجاد `AGENT_GUIDE.md` (راهنمای ایجنت بعدی).
4. **شاخه:** `arena/01a037d0-amarist`
5. **شماره کامیت:** `a60be21965385114f9ab31ff188683fc16a9ce36`
6. **اطلاعات اضافی برای ایجنت بعدی:**
   - این نسخه روی شاخهٔ موازی `arena/01a037d0-amarist` (بدون ابزار SEM) ساخته شد و در نسخهٔ ۲
     به خط اصلی ادغام شد.
   - هشدار: در `src/app/layout.tsx` خطای تایپ از پیش موجود (`LayoutProps`) بود که در آن زمان
     اصلاح نشد؛ همچنان باز است.
   - ترتیب گویه‌ها در خرده‌مقیاس‌های OGAI و AQ بر اساس ساختار متداول منتشرشده درج شده و در
     یادداشت (`note`) هر خرده‌مقیاس علامت‌گذاری شده که با متن دقیق نسخه فارسی بازبینی شود.
   - هیچ دیتابیس یا بک‌اندی در پروژه وجود ندارد (ذخیره‌سازی در localStorage).
