# راهنمای ایجنت بعدی (Agent Guide)

این فایل برای هوش مصنوعی‌های بعدی است که روی مخزن **آماریست (Amarist)** کار می‌کنند.
تجربه‌های تکمیلی را اینجا ثبت کنید تا ایجنت بعدی سریع‌تر مسیر را پیدا کند.

## ۱. محصول چیست؟
آماریست سرویس آنلاین **تحلیل آماری داده‌های پژوهشی** و **تولید داده‌های تمرینی هدفمند**
برای اساتید آمار و روش تحقیق است. بدون دیتابیس و بدون ثبت‌نام؛ ذخیره‌سازی در مرورگر
(localStorage). تکنولوژی: Next.js 16 (App Router)، TypeScript، Tailwind CSS v4، فونت وزیرمتن.

## ۲. وضعیت فعلی (تا نسخه ۱۱)
- ابزارهای زیر پیاده‌سازی شده‌اند (زیر `src/app/tools/`):
  - `/tools/sem` — تحلیل مسیر و مدل معادلات ساختاری (SEM) با استپر چندمرحله‌ای، تولید
    دادهٔ تمرینی، تحلیل دادهٔ واقعی، بوت‌استرپ ML، آلفای کرونباخ، دیاگرام و خروجی اکسل/docx/txt.
  - `/tools/alpha` — محاسبهٔ آلفای کرونباخ مستقل.
  - `/tools/one-treatment` — «اثربخشی یک مداخله» (گروه آزمایش/درمان در برابر کنترل، پیش/پس):
    t مستقل روی نمرهٔ تغییر + ANCOVA + d کوهن + بررسی پیش‌فرض‌ها.
  - `/tools/compare-treatments` — «مقایسهٔ اثربخشی دو درمان» (**سه** گروه مستقل: کنترل، درمان ۱،
    درمان ۲ × پیش/پس/پیگیری): آنوا آمیخته (گروه/زمان/تعامل) + بن‌فرونی درون‌گروهی + پیش‌فرض‌ها
    (نرمالیتی/لون/Box's M/موخلی).
  - `/tools/regression` — «پیش‌بینی رگرسیونی» (رگرسیون خطی چندگانه): ضرایب B و β استاندارد،
    R² و R² تعدیل‌شده، F، ماتریس همبستگی و تولید دادهٔ تمرینی با R² هدف.
- هر دو ابزار بالینی از یک مؤلفهٔ مشترک `src/components/clinical-tool.tsx` استفاده می‌کنند
  (prop به نام `mode: "one" | "compare"`) با پوستهٔ استپر مشترک `src/components/clinical-stepper-shell.tsx`.
  مراحل آن‌ها شامل بخش‌های: مشخصات طرح (عنوان/نوع مداخله + نام گروه‌ها با نقش)، متغیرها (متغیر
  وابسته)، سطح معناداری (آلفا)، قیود تولید، داده، تحلیل، پیش‌فرض‌ها، یافته‌های توصیفی و استنباطی (جدا)،
  گزارش و ذخیره است. کتابخانه‌ها: `src/lib/clinical-stats.ts`، `src/lib/clinical-generator.ts`،
  `src/lib/clinical-report.ts`. هر ابزار در اولین باز شدن یک پروژهٔ نمونهٔ پیش‌فرض در localStorage
  می‌سازد (seed غیرمخرب). ابزار رگرسیون از `src/components/regression-tool.tsx` + `src/lib/regression.ts`
  + `src/lib/regression-report.ts` استفاده می‌کند.
- صفحهٔ فرود (`src/components/research-modes.tsx`) پنج کارت را کنار هم نشان می‌دهد
  (اثربخشی یک مداخله، مقایسه دو درمان، تحلیل مسیر، SEM، پیش‌بینی رگرسیونی) با دکمهٔ گرادیانیِ
  «ورود به ابزار». عنوانِ بالای کارت‌ها: **«دستیار هوشمند تحلیل آماری برای اساتید و دانشجویان»**.
  گزینهٔ «مرحله پیگیری» **دیگر روی کارت نیست**؛ داخل ابزار در مرحلهٔ «تشخیص» تنظیم می‌شود.
- فازهای بعدی نقشه‌راه (در `README.md`) هنوز باز است.

## ۳. دو مفهوم «پروژهٔ پیش‌فرض» (مهم — اشتباه نگیرید!)
در این مخزن دو چیز جداگانه «پروژهٔ پیش‌فرض» نامیده می‌شوند:

### الف) پروژه‌های پیش‌فرضِ ابزار SEM (قابل مشاهده در UI)
- در `src/app/tools/sem/page.tsx` تعریف می‌شوند. هنگام باز شدن ابزار، در localStorage
  (کلید `amarist-projects`) seed می‌شوند و در لیست پروژه‌ها دیده می‌شوند.
- الگو: آرایهٔ `VariableSpec[]` (نقش/نام/نمرهٔ کل/زیرمقیاس با دامنه) + ثابت‌های
  `*_PROJECT_ID / *_PROJECT_NAME / *_PROJECT_SEED_KEY` + تابع `ensure*Project(existing)`.
- پروژه‌های فعلی: «پروژه پیش‌فرض» (عمومی)، «پروژه پیش‌فرض (شیردل)»، «پروژه پیش‌فرض (تارا رضوانی)».
- **چگونه پروژهٔ SEM جدید اضافه کنید:** یک `xxxVars: VariableSpec[]` بسازید، سه ثابت و یک
  تابع `ensureXxxProject` اضافه کنید و در `loadInitialProjects` (هر دو مسیر: حالتِ خالی و
  حالتِ موجود) صدا بزنید. seeding باید **ادغام‌پذیر و غیرمخرب** باشد: با seed-key، حذفِ
  عمدیِ کاربر را دوباره برنگردانید.

### ب) متادیتای استاتیک پروژه‌های پیش‌فرض (برای موتور فاز ۳)
- در `src/data/default-projects/` (قرارداد در `types.ts`). نخستین پروژه «تارا رضوانی»
  (`tara-razvani.ts`) شامل جزئیات کامل ۴ پرسشنامه (OGAI/ERQ/AQ/احساس ناکامی)، جمعیت،
  نمونه‌گیری، پایایی و... است. این داده‌ها هنوز به UI وصل نیستند؛ مصرف‌کنندهٔ بعدی آن‌ها
  موتور تولید دادهٔ تمرینی (فاز ۳ نقشه‌راه) خواهد بود.
- **چگونه پروژهٔ استاتیک جدید اضافه کنید:** فایل جدید در `src/data/default-projects/` بسازید
  و به آرایهٔ `defaultProjects` در `index.ts` اضافه کنید.

## ۵. قوانین گیت‌هاب و شاخه (خیلی مهم)
- **هرگز** روی `main` یا `production` کار نکنید. فقط روی یک شاخهٔ preview (مثل `arena/...`).
- هر بار فقط **یک بار** push کنید و آن هم بعد از اتمام همهٔ تغییرات (قانون ۴.۶).
- پیام کامیت/push باید **تاریخ و زمان شمسی با ساعت تهران** داشته باشد (قانون ۴.۸).
- قبل از هر کاری، اتصال به GitHub را چک کنید (قانون ۴.۷).
- مسئولیت ادغام با main به عهدهٔ کاربر است؛ شما فقط در شاخهٔ preview پوش کنید.

## ۵٫۱. مهم‌ترین درسِ فنی این مخزن: برآوردگرِ ارزیابی باید با برآوردگرِ نمایش یکی باشد

**اگر یک چیز از این فایل به خاطر بسپارید، همین است:** در ابزار SEM دو برآوردگر داریم —
`approx` (نمرهٔ مرکب/ترکیبی، خیلی ارزان: ~۴ms) و `ml` (برآوردِ هم‌زمانِ درست‌نماییِ بیشینه،
گران: ~۳۳۷ms با خطای معیار، ~۱۸۸ms بدون آن). برنامه در تحلیل نهایی همیشه از `ml` استفاده می‌کند
(`src/app/tools/sem/page.tsx`، فراخوانِ `estimateSem(..., "ml")`).

هر جایی که **داده تولید یا ارزیابی** می‌کنید، باید از همان برآوردگر استفاده کنید؛ وگرنه خروجیِ
نهایی قیود را نقض می‌کند و کاربر با پیامِ «قیود رعایت نشد» روبه‌رو می‌شود — در حالی که کد درست کار کرده است.
تفاوتِ واقعیِ اندازه‌گیری‌شده روی پروژهٔ «تارا رضوانی» (n=۳۰۰):

| شاخص | تقریبی | ML |
| --- | --- | --- |
| CFI | ۰٫۹۸۸ | **۱٫۰۰۰** |
| RMSEA | ۰٫۰۳۵۶ | **۰٫۰۰۰** |
| CMIN/df | ۱٫۳۷۸ | **۰٫۸۰۹** |
| R² گرهٔ میانجی | ۰٫۲۲۰ | **۰٫۳۳۸** |
| pِ یک مسیر | ۰٫۰۲۱۹ | **۰٫۰۸۰۱** |

بنابراین: قیدهای «واقع‌گرایانه» (سقفِ CFI/IFI/GFI و کفِ RMSEA/SRMR) فقط با ML معنا دارند،
و خروجیِ تقریبی برای سنجشِ آن‌ها بی‌ربط است.

## ۵٫۲. هزینهٔ محاسباتی (اندازه‌گیری‌شده با Node — برای برنامه‌ریزیِ هر تغییر بعدی)

| عملیات | هزینه |
| --- | --- |
| `estimateSem(...)` با `approx` | ~۴ms |
| `estimateSem(...)` با `ml` (با خطای معیار) | **~۸۰ms** (نسخهٔ ۸: گرادیانِ تحلیلی) |
| `estimateSemMl` با `computeStandardErrors: false` | **~۲۵ms** |
| بوت‌استرپ تقریبی، ۱۰۰۰ نمونه | ~۵۰۰ms |
| **بوت‌استرپ ML (تک‌رشته‌ای)** | **~۶٫۵ms به‌ازای هر نمونه** (نسخهٔ ۸؛ قبل از آن ۳۵ms و در نسخهٔ ۶ حدود ۸۵ms) |
| **بوت‌استرپ ML با ۸ رشتهٔ موازی** | **~۰٫۹ms به‌ازای هر نمونه** (۲۰۰۰ نمونه ≈ ۲ تا ۳ ثانیه) |

نتیجهٔ عملی: بوت‌استرپ ML دیگر آن‌قدر ارزان شده که می‌توان آن را پس از هر تحلیل اجرا کرد، اما
هم‌چنان در حلقهٔ تولید فقط با سقف (`SCREEN_BOOT_CAP = ۳۰۰` برای غربال و `VERIFY_BOOT_CAP = ۳۰۰`
برای راستی‌آزمایی) صدا زده می‌شود. جزئیاتِ شتاب در بخش ۵٫۸.

## ۵٫۳. معماریِ تولید داده در SEM (نسخهٔ ۵ به بعد)

- `generateSemData(input, options)` در `src/lib/sem-generator.ts` **ناهمگام (`async`)** است و بین
  تلاش‌ها با `await` به حلقهٔ رویداد فرصت می‌دهد تا پیشرفت به‌روزرسانی شود (و دستورِ توقف دریافت شود).
- دیگر **خطا پرتاب نمی‌کند**: همیشه بهترین تلاش را برمی‌گرداند همراه با
  `report: SemConstraintReport` (جدولِ وضعیتِ تک‌تکِ قیود + پیشنهادها) و `success: boolean`.
- خروجی در **Worker** (`src/workers/sem-generator.worker.ts`) اجرا می‌شود؛ صفحه قفل نمی‌شود.
  الگویِ ساختِ Worker در این مخزن: `new Worker(new URL("../../../workers/xxx.worker.ts", import.meta.url), { type: "module" })`.
- مؤلفهٔ نمایش: `src/components/sem-generation-modal.tsx` (پیشرفتِ زنده + جدولِ گزارش).
- مقایسهٔ مرزِ قیود با epsilon انجام می‌شود (`MARGIN_EPS`) تا خطای ممیز شناور باعث مردودیِ اشتباه نشود.
- پذیرش/رد با `allPassed` (بررسیِ تک‌تکِ ردیف‌ها) انجام می‌شود، **نه** با `score >= 0`.

## ۵٫۴. تم (لایت/دارک) — پیش‌فرضِ برنامه دارک است
- منبعِ حقیقت، کلاسِ `dark` روی `<html>` است؛ یک اسکریپتِ پیش از هیدریشن در `src/app/layout.tsx`
  آن را از روی `localStorage.getItem("amarist-theme") !== "light"` تنظیم می‌کند (یعنی **دارک پیش‌فرض** است).
- مؤلفهٔ `src/components/theme-toggle.tsx` با **`useSyncExternalStore`** نوشته شده است.
  **هشدار مهم:** برای خواندنِ localStorage از `useState` + `useEffect` استفاده نکنید؛ قانونِ
  `react-hooks/set-state-in-effect` در React 19 آن را **خطا** می‌داند و کلِ خروجیِ `eslint` قرمز می‌شود.
- سوییچِ تم در `ToolHeader` (ابزارها) و `Navbar` (صفحهٔ فرود) هر دو درج است.

## ۵٫۵. نام‌گذاریِ پروژه‌ها: شماره، نه نامِ شخص
- نام‌هایِ قابل‌مشاهده در UI: **«پروژه پیش‌فرض ۱»**، **«پروژه پیش‌فرض ۲»**، **«پروژه پیش‌فرض ۳»**.
- **شناسه‌ها و seed-keyهای localStorage را عوض نکنید** (مثل `default-tara-razvani-v1`)؛ تغییرِ آن‌ها برای
  کاربرانِ فعلی **پروژهٔ تکراری** می‌سازد و خاصیتِ «غیرمخرب بودنِ seeding» را از بین می‌برد.
- چون نامِ پروژه در localStorage ذخیره می‌شود، هر تغییرِ نام به یک **مهاجرتِ یک‌باره** نیاز دارد.
  الگو: `migrateDefaultProjectNames` در `src/app/tools/sem/page.tsx` — با یک seed-key جداگانه،
  فقط نام‌هایِ دقیقاً منطبق را عوض می‌کند و به نام‌های سفارشیِ کاربر دست نمی‌زند.

## ۵٫۶. بوت‌استرپِ خودکار و نوار پیشرفت
- بوت‌استرپِ اثرات مستقیم/غیرمستقیم/کل بلافاصله پس از تولید داده یا اجرای تحلیل اجرا می‌شود؛
  دکمهٔ «اجرای بوت‌استرپ» فقط برای نمونه‌گیریِ دوباره است.
- `src/workers/sem-bootstrap.worker.ts` پیام‌هایِ `{ type: "progress", done, total }` می‌فرستد و
  در پایان **نمونه‌های خام** (`{ definitions, effects }`) را برمی‌گرداند — نه ردیف‌های خلاصه‌شده.
  از نسخهٔ ۸، `runBootstrap` یک استخر از این Workerها (بر اساس `navigator.hardwareConcurrency`،
  حداکثر ۸) می‌سازد و نمونه‌ها را پیش از محاسبهٔ فاصلهٔ اطمینان ادغام می‌کند (ادغام با
  `summarizeMlIndirect` در رشتهٔ اصلی). Workerها یک «بذر» (`seed`: پارامترها، ضرایب استاندارد و
  وارونِ هسی‌ینِ برآوردِ نمونهٔ کامل) می‌گیرند تا هر کدام برآوردِ نمونهٔ کامل را دوباره انجام ندهند.
- **مراقبِ حلقهٔ بی‌انتها باشید:** `analyze` در پایان `runBootstrap` را صدا می‌زند؛ `runBootstrap`
  نباید هیچ‌وقت `analyze` را صدا بزند یا stateای را تغییر دهد که `analyze` در وابستگی‌هایش دارد.

## ۶. محافظت از دیتابیس/بک‌اند (قانون ۴.۴)
- در این پروژه دیتابیس/بک‌اندی وجود ندارد (همه‌چیز در localStorage مرورگر است). اگر روزی
  چنین چیزی اضافه شد، هر تغییری در آن را ابتدا با کاربر بحث کنید (فرمت «وضعیت اضطراری» با
  عدد ۵ رقمیِ تأیید) و بی‌اجازه اعمال نکنید.

## ۷. نکات فنی
- `AGENTS.md` توسط خودِ Next.js مدیریت می‌شود (بلوک قوانین فریمورک)؛ آن را دستی تغییر ندهید.
  راهنمای فریمورک را از `node_modules/next/dist/docs/` بخوانید.
- `src/app/layout.tsx` از `LayoutProps<"/">` استفاده می‌کند — این API معتبرِ Next.js 16 است،
  نه خطا. تایپِ آن را خودِ Next در `.next/types` (و `.next/dev/types`) تولید می‌کند. بنابراین
  در یک clone تازه، اگر قبل از اجرای `next dev` یا `next build` مستقیماً `npx tsc --noEmit`
  بزنید، خطای `TS2304: Cannot find name 'LayoutProps'` می‌بینید؛ بعد از اولین اجرای dev/build
  این خطا از بین می‌رود و `npm run build` کاملاً موفق است (در نسخهٔ ۲ تأیید شد).
- متن فارسی: نیم‌فاصله (ZWNJ) را مثل بقیهٔ کد حفظ کنید (مثلاً «بازی‌های»، «پیش‌فرض»).
- پرسشنامهٔ ERQ نمرهٔ کل ندارد؛ در SEM به‌صورت `hasTotal: false` با دو زیرمقیاس مستقل مدل می‌شود.

## ۸. آخرین وضعیت شاخه و کامیت
- **شاخهٔ فعلی (نسخهٔ ۹):** `arena/01a05635-amarist`
- **شاخهٔ قبلی (نسخه‌های ۵ تا ۸):** `arena/01a050e8-amarist` (نسخهٔ ۹ از آخرین tip آن، `4d79155`، ادامه یافت).
- شاخهٔ قدیمی‌تر (نسخه‌های ۲ تا ۴): `arena/01a04632-amarist` — ادغامِ دو خط کاری قبلی است:
  - خط ابزار SEM: `arena/019ff114-amarist` → `arena/01a00a94-amarist` (آخرین: `8bf7abe` —
    کاربر آن را «آخرین و بی‌نقص» معرفی کرد).
  - خط پروژه‌های پیش‌فرض استاتیک: `arena/01a037d0-amarist` (نسخه ۱: `a60be21`).
- شماره کامیت نسخه ۲: `73f659769357af6efc50cd3a12eb0be209aa7b1a` — ۱۴۰۵/۰۶/۰۶ (تهران)
- شماره کامیت نسخه ۳ (به‌روزرسانی بالینی): `13c46ce52e2c451b6a1e5fd14fec66d49755dd60` — ۱۴۰۵/۰۶/۰۷ (تهران).
- شماره کامیت نسخه ۴ (بازبینی بالینی + ابزار رگرسیون): `3dc0d802055d47d6aa016f9b7949a741b6546269` — ۱۴۰۵/۰۶/۰۷ (تهران).
- شماره کامیت نسخه ۷ (اصلاحِ موتورِ تولید): `9e7de301b94c102418f80105253feff561043e3a` — ۱۴۰۵/۰۶/۰۸ (تهران).
- شماره کامیت نسخه ۸ (زیرمقیاس در اثرات غیرمستقیم + شتابِ بوت‌استرپ): `330ee7cf040a9fc11aebfb7096e004163c407784`.
- شماره کامیت کد نسخه ۹ (قیود کامل + β + همبستگی + آلفا): `0155565d9fb49dfe8dd611fd954ca4d595aed0eb` — ۱۴۰۵/۰۶/۰۹ — ۰۸:۵۵ (تهران).
- جزئیات بیشتر در `VERSION_LOG.md`.

## ۹. «دربارهٔ برنامه» و نسخه‌گذاری (قانون ۴٫۸)
- تنها مرجعِ اطلاعاتِ نسخه: **`src/data/app-version.ts`**. هنگام هر push باید این فایل به‌روزرسانی شود:
  شمارهٔ نسخه، زمانِ انتشار (هجری شمسی، ساعت تهران)، شاخه، شمارهٔ کامیت و خلاصهٔ تغییرات.
- مؤلفهٔ نمایش: `src/components/about-app.tsx`؛ در `ToolHeader` (همهٔ ابزارها) و `Footer` (صفحهٔ فرود) درج شده است.
- ترتیبِ درستِ کار: ابتدا همهٔ تغییرات را انجام بده → `VERSION_LOG.md` و `AGENT_GUIDE.md` را به‌روزرسانی کن →
  commit بزن → شمارهٔ کامیت را در `app-version.ts` و `VERSION_LOG.md` بنویس → `git commit --amend` →
  **یک بار** push (قانون ۴٫۶: فقط یک push در پایانِ همهٔ تغییرات).

## ۱۰. چطور موتور تولید را با Node صحت‌سنجی کنیم (بدون مرورگر)
موتور تولید و کتابخانه‌های آماری هیچ وابستگی به مرورگر ندارند (فقط importهای نسبی، بدون alias)،
بنابراین می‌توان آن‌ها را مستقیماً با `tsx` اجرا کرد. این کار زمانی انجام شده که موتور تغییر کرده است:

```bash
npx --yes tsx /tmp/yourtest.mts          # برای فایل‌های بدون alias
npx --yes tsx --tsconfig ./tsconfig.json /tmp/yourtest.mts   # وقتی نیاز به alias @/ دارید
```

نکته‌ها:
- فایلِ تست را با پسوندِ **`.mts`** بنویسید تا `await` در سطحِ بالا مجاز باشد (با `.ts` خطای esbuild می‌گیرید).
- برای تستِ Worker، یک شیءِ مصنوعیِ `self` با `onmessage`/`postMessage` روی `globalThis` بسازید و
  سپس ماژولِ Worker را `import` کنید (نمونه در تاریخچهٔ این مخزن استفاده شده و کار می‌کند).
- سناریوهایی که همیشه باید تست شوند: (۱) قیودِ پیش‌فرضِ پروژهٔ «تارا رضوانی»، (۲) قیودِ روی مسیرها
  (معنادار/غیرمعنادار + بازهٔ β)، (۳) دادهٔ گمشده/پرت، (۴) یک قیودِ عمداً غیرممکن (باید بهترین تلاش را
  همراه با گزارش برگرداند، نه استثنا).

## ۵٫۷. درس‌های نسخهٔ ۷: چهار تلهٔ موتورِ تولید داده (اگر یک چیز را به خاطر بسپارید، این است)

این چهار تله هر چهار تا باعث شده بود خروجیِ برنامه از نظرِ علمی نامعتبر شود، در حالی که هیچ
خطایی رخ نمی‌داد و همهٔ تست‌ها سبز بودند. قبل از هر تغییری در `sem-generator.ts` آن‌ها را بخوانید.

### ۵٫۷٫۱. پیش‌فرضِ رابط کاربر باید با پیش‌فرضِ موتور یکی باشد

رابط کاربر پیش‌فرضِ هر مسیر را «معنی‌دار باشد» نشان می‌داد، اما موتور می‌گفت
«اگر کاربر چیزی تنظیم نکرده، قیدی وجود ندارد» و آن را کلاً رد می‌کرد. قانون:
**هرچه رابط کاربر به عنوان پیش‌فرض نشان می‌دهد، موتور باید همان را بسنجد.**
قیدِ مؤثر با `resolvePathTargetForArrow` استنتاج می‌شود.

چون سنجشِ معناداری نیازمند خطای معیار (هسین عددی، ~۱۵۰ms اضافه) است، ارزیابی **دومرحله‌ای**
شده است: برازشِ ارزانِ بدون خطای معیار برای همهٔ تلاش‌ها، و برازشِ کامل فقط برای نامزدهایی که
از غربال رد شده‌اند (`needsPathChecks` و `skipPaths`).

### ۵٫۷٫۲. خطای اندازه‌گیری فقط برای گره‌های چندشاخص

`compileModel` در `sem-ml.ts` می‌گوید `latent: columns.length > 1`؛ یعنی گره‌ای که یک شاخص
دارد (زیرمقیاس‌های متغیر غیرجمع‌پذیر مانند ERQ) «مشاهده‌شدهٔ بدون خطا» است. اگر مولد برای
چنین گره‌ای خطای اندازه‌گیری اعمال کند، β برآوردشده ≈ β هدف × λ می‌شود و مسیرها غیرمعنادار
می‌شوند. در مولد: `const lam = v.hasTotal ? rand(0.6, 0.85) : 1;`

### ۵٫۷٫۳. پیش‌بین‌های برون‌زا باید همبسته باشند

تولیدِ نمراتِ نهفتهٔ **مستقل** باعث می‌شد ماتریس همبستگیِ پیش‌فرض‌ها مصنوعی (همه صفر) باشد.
`buildExogenousCorrelation` علامت را از قطبیتِ سازه‌ها می‌گیرد. توجه: با پیش‌بین‌های همبسته،
واریانسِ ترکیبِ خطی دیگر برابرِ Σb² نیست و باید با جملاتِ متقاطع محاسبه شود — وگرنه β
برآوردی با β هدف فرق می‌کند. ماتریس `covLat` برای همین به‌صورت بازگشتی به‌روزرسانی می‌شود.

### ۵٫۷٫۴. امتیاز = کمینهٔ حاشیه، پس «قیدِ کمتر» برنده می‌شود

`score = Math.min(...checks.map(c => c.margin))`. نامزدی که قیدهای کمتری دارد امتیازِ بهتری
می‌گیرد. برای همین `bestFull` جدا نگه داشته می‌شود و گزارش از `bestFull ?? best` ساخته می‌شود.

### ۵٫۷٫۵. عددهایِ کلیدی (اندازه‌گیری‌شده با Node، n=۳۰۰، مدل ۴ سازه / ۱۰ شاخص)

| عملیات | قبل از نسخهٔ ۷ | بعد از نسخهٔ ۷ |
| --- | --- | --- |
| بوت‌استرپ ML | ~۸۵ms به‌ازای هر نمونه | **~۳۵ms** (۱۵ تکرار، آستانه ۱e-۴) |
| ۲۰۰۰ نمونهٔ بوت‌استرپ | ~۱۷۰ ثانیه | **~۷۰ ثانیه** |
| ۵۰۰۰ نمونه (پیش‌فرضِ قدیم) | ~۷ دقیقه | دیگر پیش‌فرض نیست |

## ۵٫۸. درس‌های نسخهٔ ۸: اثرِ غیرمستقیمِ هر زیرمقیاس، گرادیانِ تحلیلی و اجرای موازی

سه گزارشِ کاربر در نسخهٔ ۸: (۱) جدولِ اثرات غیرمستقیم برای ERQ یک ردیف نشان می‌داد در حالی که
این پرسشنامه نمرهٔ کل ندارد؛ (۲) امکان نبود برای هر زیرمقیاس جداگانه «منفی و معنادار» یا
«مثبت و معنادار» خواست؛ (۳) بوت‌استرپ هنوز کند بود. ریشه و درمان هر سه در ادامه است.

### ۵٫۸٫۱. «اثرِ کلِ متغیر» برای پرسشنامهٔ غیرجمع‌پذیر تعریف ندارد

جمعِ جبریِ دو زیرمقیاسِ ناهمسو یکدیگر را خنثی می‌کند (ارزیابی مجدد ۰٫۰۷۵− و فرونشانی ۰٫۰۸۴+ ⇒
خالص ۰٫۰۰۹ با p≈۰٫۸۵!)؛ یعنی ردیفِ «کلِ متغیر» هم گمراه‌کننده است و هم بی‌معنا. قانون:
**واحدِ اثر برای متغیرِ جمع‌پذیر «کل متغیر» و برای متغیرِ غیرجمع‌پذیر «هر زیرمقیاس» است.**
پیاده‌سازی در `buildIndirectUnits` (در `sem-ml.ts`) و یک قانونِ ساده: هر متغیری که بیش از یک گره
دارد غیرجمع‌پذیر است (متغیرِ جمع‌پذیر دقیقاً یک گره به نامِ «نام (کل)» دارد). ردیف‌ها سه فیلدِ
`fromNode`/`viaNode`/`toNode` گرفته‌اند که فقط برای زیرمقیاس‌ها پر می‌شوند؛ برچسبِ جدول و خروجیِ
Word/گزارش متنی با `bootPathLabelOf` از همان واحد استفاده می‌کنند. در سمتِ مولد، قیدِ غیرمستقیم
برای هر زیرمقیاس جداگانه سنجیده می‌شود (`bootRowOf(fromNode?.nodeId ?? null)`) و دیگر نیازی به
چشم‌پوشی از معناداریِ «اثر خالص» نیست.

### ۵٫۸٫۲. گرادیانِ تحلیلیِ تابعِ هدفِ ML (بزرگ‌ترین شتابِ این نسخه)

بدون گرادیانِ تحلیلی، هر تکرارِ BFGS حدود (۲p+۲) ارزیابیِ تابعِ هدف هزینه دارد (p≈۳۰ ⇒ ~۶۶
ارزیابی). با جای‌گذاری Σ = Λ·Σ_L·Λᵗ + Θ و Σ_L = A·Ψ·Aᵗ و G = Σ⁻¹(Σ−S)Σ⁻¹ داریم
`dF/dθ = tr(G·Σ̇)` و با قرار دادن K = Λᵗ·G·Λ:

- ضرایب مسیرِ `B[to][from]`: `dF/db = 2·(Σ_L·K·A)[from][to]`
- بار عاملیِ `Λ[o][k]`: `dF/dλ = 2·(G·Λ·Σ_L)[o][k]`
- واریانس خطا: `G[o][o]·exp(θ)` و واریانس اغتشاش: `(Aᵗ·K·A)[k][k]·exp(ψ)`
- درایه‌های چولسکیِ برون‌زا: `2·(W_ex·L)[i][j]` (و برای قطر × `exp`)

**شیوهٔ صحت‌سنجی (حتماً تکرار کنید):** `analyticGradient` را در برابر تفاضلِ مرکزی در چند نقطه
(بهینه، مقدار اولیه، و سه نقطهٔ تصادفیِ آشفته) مقایسه کنید؛ خطایِ مجاز ≈ ۵e-۹ مطلق است. کدِ
نمونه در تاریخچهٔ این گفت‌وگو (`/tmp/semtest/gradcheck.js`). اگر گرادیان غلط باشد، برنامه خطا
نمی‌دهد — فقط به نقطهٔ بهینهٔ غلط همگرا می‌شود.

### ۵٫۸٫۳. شروعِ گرمِ وارونِ هسی‌ین (۳۸ تکرار → ۹ تکرار، با همان پاسخ)

هر نمونهٔ بوت‌استرپ BFGS را با وارونِ هسی‌ینِ همانی شروع می‌کرد، چون `estimateSemMl` تقریبِ هسی‌ین
را بین نمونه‌ها منتقل نمی‌کرد. حالا `estimateSemMl` مقدارِ `inverseHessian` را برمی‌گرداند و
بوت‌استرپ آن را به عنوان `startInverseHessian` می‌دهد. جهتِ جست‌وجو فقط **سرعت** را عوض می‌کند، نه
پاسخ را؛ اندازه‌گیری روی یک نمونه‌گیریِ یکسان: همبستگیِ نمونه‌به‌نمونه با اجرایِ کاملاً همگرا
(آستانهٔ ۱e-۸) برابر **۱٫۰۰۰۰** است.

### ۵٫۸٫۴. نمونهٔ ناسالمِ بوت‌استرپ (`MAX_BOOTSTRAP_STD = ۱٫۵`)

در داده‌های با هم‌خطیِ شدید، یک نمونهٔ بازنمونه‌گیری‌شده می‌تواند ضرایبِ استانداردِ ناممکن تولید
کند و فاصلهٔ اطمینان را به‌کل بی‌معنا کند (مثالِ واقعی: اثر غیرمستقیمِ ۱٫۴ برای β≈۰٫۱۵). چنین
نمونه‌ای کنار گذاشته می‌شود و تعدادش با فیلدهای `usable`/`requested` در جدول گزارش می‌گردد
(رفتاری مشابه AMOS با نمونه‌های ناهمگرا).

### ۵٫۸٫۵. قیدِ اختصاصیِ زیرمقیاس با کلیدِ «برچسبی» (نه عددی)

`constraints.nodePathTargets` با کلیدِ `` `${fromLabel}→${toLabel}` `` (ساخته‌شده با `nodePathKey`)
ذخیره می‌شود. دلیل: شمارهٔ گره‌ها با هر تغییر در فهرستِ متغیرها/زیرمقیاس‌ها عوض می‌شود و قیدِ
کاربر را بی‌صدا خاموش می‌کند، اما برچسب پایدار است. اولویت در `resolvePathTargetForArrow`:
**سطحِ زیرمقیاس ← سطحِ متغیر ← استنتاج از قطبیتِ سازه**. در رابط کاربر، ردیفِ «همهٔ زیرمقیاس‌ها»
مقادیر را به ردیف‌های زیرمقیاس ارث می‌دهد تا وقتی که خودشان تنظیم شوند.

> **تغییر نسخهٔ ۹:** ردیف «همهٔ زیرمقیاس‌ها» حذف شد، چون برای متغیرِ بدون نمرهٔ کل از نظر مفهومی معتبر نیست؛ این بند فقط تاریخچهٔ رفتار نسخهٔ ۸ است.

### ۵٫۸٫۶. عددهایِ کلیدیِ نسخهٔ ۸ (Node، n=۳۰۰، مدل ۴ سازه / ۱۰ شاخص)

| عملیات | نسخهٔ ۶ | نسخهٔ ۷ | **نسخهٔ ۸** |
| --- | --- | --- | --- |
| بوت‌استرپ ML (هر نمونه) | ~۸۵ms | ~۳۵ms | **~۶٫۵ms** (و کاملاً همگرا: آستانه ۱e-۶) |
| ۲۰۰۰ نمونه، تک‌رشته | ~۱۷۰s | ~۷۰s | **~۱۳s** |
| ۲۰۰۰ نمونه، ۸ رشتهٔ موازی | — | — | **~۲ تا ۳ ثانیه** |
| تولید کامل (۲۰۰ تلاش + راستی‌آزمایی) | ~۳ دقیقه | ~۶۰s | **~۲۱ ثانیه** |
| تعداد ردیف‌های اثر غیرمستقیم برای ERQ | ۱ (خالص، غیرمعنادار) | ۱ | **۲ (هر زیرمقیاس، هر دو معنادار)** |

## ۵٫۹. درس‌های نسخهٔ ۹: واحد اثر در قیود، fallback سه‌حالته و آلفای زیرمقیاس‌محور

### ۵٫۹٫۱. قانون «واحد اثر» باید در رابط و موتور یکی باشد

در نسخهٔ ۸ خروجیِ اثر غیرمستقیم برای ERQ درست و زیرمقیاس‌محور بود، اما جدولِ قیود هنوز یک ردیف
در سطح «تنظیم هیجان» نشان می‌داد. قانون کامل نسخهٔ ۹: **متغیرِ بدون نمرهٔ کل در هیچ جدولِ اثر یا
قید ردیف کلی ندارد**؛ فقط زیرمقیاس‌هایش واحد مستقل‌اند. `indirectUnitsOfVar(nodes, varId)` این
قاعده را در مبدأ، میانجی و مقصد اعمال می‌کند. جدول مستقیم نیز ردیف «همهٔ زیرمقیاس‌ها» ندارد.

کلید قید غیرمستقیم باید برچسب‌محور باشد: `indirectUnitKey` برای مسیر `p∷…` و برای کل اثر `t∷…`
می‌سازد. ابتدا این کلید خوانده می‌شود و فقط برای سازگاری، کلید عددی قدیمی fallback است.

### ۵٫۹٫۲. در قید β سه حالت داریم، نه دو حالت

- `undefined`: کاربر هرگز تنظیم نکرده است؛ پیش‌فرضِ ۰٫۱۰ یا ۰٫۵۰ اعمال شود.
- `null`: کاربر عمداً فیلد را خالی کرده است؛ آن سمتِ قید حذف شود.
- `number`: مقدار صریح کاربر.

عملگر `??` دو حالت اول را یکی می‌کند و این قرارداد را می‌شکند. موتور با `pickBeta` و رابط با
`pickSet` نخستین مقدارِ **غیر-undefined** را انتخاب می‌کنند. پیش‌فرضِ الزامیِ |β| برابر ۰٫۱۰ تا
۰٫۵۰ است.

### ۵٫۹٫۳. همبستگی برون‌زا فقط مثبت‌معین بودن نیست

کف نهفتهٔ ۰٫۱۵ پس از خطای اندازه‌گیری به حدود ۰٫۱۲ می‌رسید و در n=۳۰۰ گاهی معنادار نبود. کف به
۰٫۲۸ افزایش یافت. ماتریس علاوه بر چولسکی باید `maxVifOfCorrelation(matrix) <= 4` را پاس کند؛
VIF از قطرِ وارون ماتریس همبستگی محاسبه می‌شود. گزینهٔ `enforceExogCorr` (پیش‌فرض روشن) معناداری
همهٔ جفت‌های برون‌زا را در دادهٔ مشاهده‌شده کنترل می‌کند و نتیجه در بخش ۵-۴ گزارش می‌شود.

### ۵٫۹٫۴. کنترل‌کننده‌های مولد را با هم ادغام نکنید

چهار Map مستقل‌اند: `arrowScale` (نسبت β برآوردی به ضریب خام برای هر فلش)، `arrowBoost`
(معناداری)، `arrowTrim` (کف/سقف β) و `arrowMediation` (کف اثر غیرمستقیم). تقویت میانجی هرگز کاهش
نمی‌یابد. ابتدا boostها در سقف بالشتک‌دار مهار و سپس trim اعمال می‌شود. تعداد تلاش پیش‌فرض ۳۰۰ است.

### ۵٫۹٫۵. قرارداد آلفای کرونباخ داخل SEM

`AlphaItem` علاوه بر دامنه، فیلد `sub` دارد. نام زیرمقیاس‌ها و `hasTotal` از متغیرهای پروژه می‌آید.
هر زیرمقیاس یک آلفای مستقل دارد؛ گروه کل فقط وقتی ساخته می‌شود که `hasTotal: true` باشد. قرارداد
ستون اکسل «متغیر — زیرمقیاس — گویهٔ N» است. دو منبع داده مستقل‌اند: تمرینی و واقعی. n از تعداد
ردیف‌ها تشخیص داده می‌شود و فیلد خالی یعنی خودکار.

توابع دارای `Math.random()` باید در سطح ماژول بمانند؛ انتقالشان به بدنهٔ کامپوننت قانون
`react-hooks/purity` را نقض می‌کند. برای تولید زیرمقیاس‌ها از مؤلفهٔ مشترک + اختصاصی با rho بین
۰٫۲۵ تا ۰٫۷۵ استفاده می‌شود تا آلفای کلِ پرسشنامه‌های جمع‌پذیر مصنوعاً سقوط نکند.

### ۵٫۹٫۶. نتایج صحت‌سنجی نسخهٔ ۹

- TypeScript: موفق (پس از تولید تایپ‌های Next).
- ESLint: صفر خطا و شش هشدار قدیمی و بی‌ربط.
- Next build: موفق، ۹ صفحهٔ استاتیک.
- HTTP: مسیرهای `/`، `/tools/sem` و `/tools/alpha` همگی ۲۰۰.
- مدل واقعی ۴ سازه/۱۰ شاخص/n=۳۰۰: چهار اجرای پیاپی موفق، ۷۲ از ۷۲ قید؛ حدود ۱۳ تا ۲۲ ثانیه.
- آزمایش تولید آلفا: ۱۰ از ۱۰ اجرا برای همهٔ زیرمقیاس‌ها و کل‌های مجاز موفق، در چند میلی‌ثانیه.


## ۵٫۱۰. درس‌های نسخهٔ ۱۰: جهت غیرمستقیم و تولید گویهٔ مشروط به جمع SEM

### ۵٫۱۰٫۱. جهت خودکار اثر غیرمستقیم باید از قیدهای مؤثر فلش‌ها بیاید

اثر غیرمستقیم حاصل‌ضرب دو ضریب مسیر است. `resolveAutomaticIndirectDirection` برای هر واحد مسیر،
`resolvePathTargetForArrow` را روی فلش مبدأ→میانجی و میانجی→مقصد اجرا می‌کند؛ دو جهت یکسان یعنی
اثر مثبت و دو جهت مخالف یعنی اثر منفی. این روش تغییرهای صریح کاربر در قید مسیر مستقیم را نیز لحاظ
می‌کند. برای کل اثرِ چند میانجی، اگر جهت مسیرها ناهمسو باشد نتیجه `any` است. مقدار `dir` در
`IndirectConstraint` اختیاری است: `undefined` یعنی خودکار؛ `pos`/`neg`/`any` انتخاب صریح‌اند.

جهت داخل `evaluateAttempt` به `ConstraintCheck` تبدیل می‌شود. بنابراین صرفاً UI نیست: حاشیهٔ منفی
امتیاز تلاش را منفی و آن را مردود می‌کند. تست کنترل نسخهٔ ۱۰، مدل مثبت×منفی را با جهت منفی پاس و
با اجبار جهت مثبت پس از ۱۰۰ تلاش رد کرد.

### ۵٫۱۰٫۲. منبع حقیقت آلفا فرم فشرده است، نه آرایهٔ item-by-item

فایل `src/lib/sem-alpha.ts` قرارداد جدید را نگه می‌دارد:

- `itemCount`: تعداد صریح کل گویه؛
- `assignments[subscale]`: عبارت فهرست/بازه مانند `1-5,7,9-12`؛
- `defaultMin/defaultMax`: دامنهٔ مشترک؛
- `rangeExceptions`: بخش‌های `selector=min-max` جداشده با `;`، مانند `3=0-4; 7-9=1-7`؛
- `items`: فقط نمایش materialized و سازگاری ذخیره‌سازی قدیمی.

قبل از هر تولید، محاسبه، ایمپورت، ساخت قالب یا عبور از مرحله، `materializeAlphaScales` باید اجرا
شود. آن تابع missing، duplicate، outside و malformed را با شمارهٔ دقیق گزارش می‌کند و برای هر
زیرمقیاس حداقل دو گویه می‌خواهد. ارقام فارسی و عربی نیز نرمال می‌شوند.

### ۵٫۱۰٫۳. گویهٔ آموزشی آلفا دیگر داده‌ای مستقل نیست

`generateAlphaTrainingData` به‌جای `n`، شیء `AlphaSemTargets` می‌گیرد. صفحه برای دادهٔ تولیدی ستون
هم‌نام و برای دادهٔ واقعی نگاشت `colMap` را می‌خواند. اگر داده/ستون SEM موجود نباشد، تولید ممنوع
است. برای هر پاسخ‌دهنده، ابتدا یک تخصیص متوازن داخل دامنه ساخته می‌شود و سپس انتقال‌های جفتی
`-1/+1` نویز لازم را اضافه می‌کنند؛ این انتقال‌ها جمع را تغییر نمی‌دهند. در نتیجه:

- جمع گویه‌های هر زیرمقیاس دقیقاً برابر همان نمرهٔ موجود SEM است؛
- برای `hasTotal: true` جمع همهٔ گویه‌ها دقیقاً برابر جمع زیرمقیاس‌ها/نمرهٔ کل مدل است؛
- برای `hasTotal: false` (مثل ERQ) هیچ گروه کل ساخته نمی‌شود؛
- آلفای همهٔ زیرمقیاس‌ها و کل‌های مجاز باید داخل بازهٔ انتخابی باشد.

اگر هدف خارج از مجموع دامنه‌ها، خالی یا اعشاریِ ناسازگار باشد، یا ترکیب جمع دقیق و آلفا شدنی
نباشد، تابع خطا می‌دهد و هیچ خروجی برنمی‌گرداند. پیام ناسازگاری نزدیک‌ترین آلفای یافته‌شده را نیز
نشان می‌دهد. **هرگز این خطا را با بازکردن پنهانی بازه یا جعل موفقیت دور نزنید.**

### ۵٫۱۰٫۴. ابطال خروجی کهنه ضروری است

تغییر `source/vars/columns/rows/colMap` خروجی آموزشی آلفا را پاک می‌کند. تغییر عضویت یا دامنهٔ
گویه نیز همین کار را انجام می‌دهد و نتیجهٔ واقعی را برای محاسبهٔ مجدد نامعتبر می‌کند. هنگام
`applyProjectData` snapshot منبع هم‌زمان تنظیم می‌شود تا خروجی معتبرِ ذخیره‌شدهٔ همان پروژه بی‌جهت
پاک نشود. جریان فایل واقعی آلفا مستقل باقی می‌ماند.

### ۵٫۱۰٫۵. مهاجرت نسخهٔ ۹ و نتایج صحت‌سنجی

تنظیم item-by-item قدیمی فقط اگر مجموع min/max گویه‌های هر واحد دقیقاً با دامنهٔ امتیاز متغیر
بخواند حفظ می‌شود؛ در غیر این صورت چیدمان از دامنه‌ها استنتاج می‌شود. این کار تنظیم پیش‌فرض قدیمیِ
سه گویه‌ای را که نمی‌توانست نمره‌های ۱۰ تا ۵۰ را بازسازی کند اصلاح می‌کند.

- TypeScript پس از `next typegen`: موفق.
- ESLint: صفر خطا، شش هشدار قدیمی و نامرتبط.
- Next production build: موفق، ۹ مسیر استاتیک.
- HTTP توسعه: `/tools/sem` پاسخ ۲۰۰.
- جهت: خودکار مثبت×منفی → منفی/pass؛ اجبار مثبت روی همان مدل → fail پس از ۱۰۰ تلاش.
- آلفا: ۱۶ بررسی parser/coverage/exception/exact-sum/failure موفق؛ ۱۲۰/۱۲۰ جمع دقیق و آلفاهای
  نمونه داخل ۰٫۷۰ تا ۰٫۹۰. ناسازگاری مجموع دامنه و نبود دادهٔ SEM نیز صریحاً رد شد.
- هیچ دیتابیس یا بک‌اندی اضافه یا تغییر نکرده است.


## ۵٫۱۱. درس‌های نسخهٔ ۱۱: جدول‌های تشخیصی باید در سطح «مدل» و «پیش‌بین» تفکیک شوند

### ۵٫۱۱٫۱. VIF/Tolerance متعلق به هر پیش‌بین است؛ DW متعلق به کل معادله

جدول قدیمی یک ردیف برای هر متغیر وابسته داشت و نام همهٔ پیش‌بین‌ها، همهٔ VIFها و همهٔ
Toleranceها را در سلول‌های لیستی می‌گذاشت. این ساختار خواندن تطابق مقدار با پیش‌بین را سخت
می‌کرد و دوربین–واتسون را نیز کنار شاخص‌هایی قرار می‌داد که سطح گزارش متفاوتی دارند.

نسخهٔ ۱۱ دو جدول می‌سازد:

1. **عدم هم‌خطی چندگانه:** یک ردیف برای هر پیش‌بین با `Tolerance = 1 / VIF`؛ معیار
   `VIF < 5` و `Tolerance > 0.20`.
2. **استقلال خطاها:** یک ردیف برای هر معادله/متغیر وابسته با دوربین–واتسون؛ معیار ۱٫۵ تا ۲٫۵.

در HTML سلول متغیر وابسته با `rowSpan` گروه‌بندی می‌شود. Word به‌دلیل استفاده از جدول عمومی
`docTable` نام وابسته را در هر ردیف تکرار می‌کند، اما همچنان هر مقدار ستون مستقل دارد. گزارش متنی
نیز دو زیربخش ۶-۱ و ۶-۲ دارد.

### ۵٫۱۱٫۲. منبع واحد ردیف‌های تشخیصی

`src/lib/sem-diagnostics.ts` و تابع `buildSemRegressionDiagnostics(nodes, sem)` منبع واحد UI،
متن و Word است. ترتیب VIFها با ترتیب `sem.paths.filter(path => path.to === dependentNodeId)`
همسان است، زیرا `estimateSem` نیز pathها و VIFها را از ترتیب یکسان فلش‌های ورودی می‌سازد.

برای نمایش پایان‌نامه‌ای، پسوند `(کل)` از سازهٔ جمع‌پذیر حذف می‌شود و برای گره زیرمقیاس فقط نام
خود زیرمقیاس نمایش داده می‌شود. محاسبه و شناسهٔ گره تغییر نمی‌کند.

### ۵٫۱۱٫۳. complete cases در VIF باید با معادلهٔ وابسته یکی باشد

SPSS جدول Collinearity Statistics را روی پرونده‌هایی محاسبه می‌کند که در همان مدل رگرسیون معتبرند.
نسخهٔ قبلی، رگرسیون‌های کمکی VIF را فقط با missing پیش‌بین‌ها پاک می‌کرد و missing متغیر وابسته را
در نظر نمی‌گرفت. اکنون `estimateSem` ابتدا `completeIndices` مدل وابسته را می‌سازد و
`vifForPredictors` را روی predictorهای فیلترشده با همان شاخص‌ها اجرا می‌کند. بدون missing هیچ عددی
تغییر نمی‌کند؛ با missing، خروجی با SPSS سازگارتر است.

مقدار DW غیرمتناهی/تعریف‌نشده نباید pass شود. `buildSemRegressionDiagnostics` فقط DW متناهی در
بازهٔ ۱٫۵ تا ۲٫۵ را قبول می‌کند.

### ۵٫۱۱٫۴. راستی‌آزمایی نسخهٔ ۱۱

- دادهٔ نمونهٔ کاربر: Toleranceهای `0.780, 0.673, 0.649, 0.720` و VIFهای
  `1.282, 1.485, 1.542, 1.388` با نگاشت صحیح به چهار پیش‌بین بازسازی شد؛ DW=`2.11` فقط یک‌بار
  برای معادلهٔ پرخاشگری ساخته شد.
- آزمون مصنوعی missing: VIF موتور با محاسبهٔ جداگانه روی complete cases تا کمتر از `1e-10`
  اختلاف داشت.
- TypeScript: موفق؛ ESLint: صفر خطا و شش هشدار قدیمی؛ Next build: موفق (۹ مسیر استاتیک)؛
  `/tools/sem`: HTTP 200.
- هیچ دیتابیس یا بک‌اندی تغییر نکرده است.
