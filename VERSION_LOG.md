# فایل مدیریت نسخه‌ها (Version Management)

> این فایل تاریخچهٔ نسخه‌های کد را ثبت می‌کند تا ایجنت بعدی دقیقاً بداند آخرین وضعیت چیست.
> هر بار که تغییر مهمی انجام شد، یک ورودی جدید در بالای بخش «نسخه‌ها» اضافه کنید.

## آخرین نسخه (خلاصه سریع برای ایجنت بعدی)

- **آیا آخرین نسخه است؟** بله (نسخهٔ ۱۲، در شاخهٔ preview فعلی)
- **شاخه:** `arena/01a05635-amarist`
- **شماره کامیت کد نسخه ۱۲:** `4c7866697653d8a8431f46ecb2967fc4ed09a5bd`
- **تاریخ/زمان انتشار (تهران، شمسی):** ۱۴۰۵/۰۶/۱۰ — ۱۱:۱۷ (تهران)
- **خلاصه:** محاسبه و تفسیر کشیدگی چندمتغیری مردیا با AMOS همسان شد: فاصله‌های مورد استفادهٔ
  مردیا به مقیاس کوواریانس ML تبدیل می‌شوند، مرکز نمونهٔ محدود اعمال می‌شود و C.R. با آستانهٔ
  بزرگ‌نمونهٔ ۱٫۹۶ تفسیر می‌شود. جدول عمومی ماهالانوبیس عمداً بدون تغییر باقی مانده است.
- **نسخهٔ قبلی:** ۱۱ — کامیت `aac361dd21615d12f8a542dbe9f15f73f9f09649` — ۱۴۰۵/۰۶/۱۰ — ۰۹:۰۴ (تهران).

## نسخه‌ها

### نسخه ۱۲ — همسان‌سازی کامل کشیدگی مردیا و C.R. با AMOS

1. **تاریخ و زمان انتشار (تهران، شمسی):** ۱۴۰۵/۰۶/۱۰ — ۱۱:۱۷ (تهران)
2. **آیا این آخرین نسخه است:** بله.
3. **توضیح مختصر تغییرات نسبت به قبلی:**
   - `mardiaTest` فاصله‌هایی را که از کوواریانس نمونه‌ای با مخرج `n−1` به دست آمده‌اند، فقط برای
     محاسبهٔ مردیا در `n/(n−1)` ضرب می‌کند تا با فاصله‌های مبتنی بر کوواریانس ML در AMOS هم‌مقیاس شوند.
   - مرکز مجانبی `p(p+2)` با مرکز نمونهٔ محدود AMOS یعنی `p(p+2)(n−1)/(n+1)` جایگزین شد؛ خطای
     معیار همان `sqrt(8p(p+2)/n)` باقی ماند.
   - معیار نتیجه‌گیری C.R. از آستانهٔ عمومی ۵ به تفسیر بزرگ‌نمونهٔ AMOS یعنی `|C.R.| ≤ 1.96`
     تغییر کرد و ثابت مشترک `AMOS_MARDIA_CR_LIMIT` از پراکندگی عدد جادویی جلوگیری می‌کند.
   - رابط کاربری، گزارش متنی، فایل Word و راهنمای پیش‌فرض‌ها اکنون صریحاً روش سازگار با AMOS و
     فرمول نمونهٔ محدود را بیان می‌کنند.
   - جدول عمومی فاصله‌های ماهالانوبیس، p-valueها و فهرست موارد علامت‌گذاری‌شده تغییر نکرد؛ تبدیل
     مقیاس فقط داخل مردیا انجام می‌شود.
4. **شاخه:** `arena/01a05635-amarist`
5. **شماره کامیت کد:** `4c7866697653d8a8431f46ecb2967fc4ed09a5bd`
6. **نکته/راهنمایی برای ایجنت بعدی:**
   - ترتیب عملیات مهم است: ابتدا هر فاصله را در `n/(n−1)` ضرب کنید، سپس مربع بگیرید و میانگین را
     محاسبه کنید؛ در پایان مرکز نمونهٔ محدود را کم کنید.
   - مرجع واقعی کاربر با `n=300` و `p=10`: خروجی قدیمی آماریست حدود `−3.51` بود؛ فرمول جدید
     `−1.932157` و `C.R.=−1.080109` می‌دهد که با AMOS (`−1.932` و `−1.080`) منطبق است.
   - تست مصنوعی فرمول، TypeScript، ESLint (صفر خطا؛ شش هشدار قدیمی)، Next production build
     (۹ مسیر استاتیک) و HTTP مسیر `/tools/sem` موفق بودند. هیچ دیتابیس یا بک‌اندی تغییر نکرد.


### نسخه ۱۱ — تفکیک جدول‌های هم‌خطی و استقلال خطاها و همسان‌سازی VIF با SPSS

1. **تاریخ و زمان انتشار (تهران، شمسی):** ۱۴۰۵/۰۶/۱۰ — ۰۹:۰۴ (تهران)
2. **آیا این آخرین نسخه است:** خیر (نسخهٔ ۱۲ آخرین است).
3. **توضیح مختصر تغییرات نسبت به قبلی:**
   - جدول ترکیبی قبلی که نام همهٔ پیش‌بین‌ها و چند مقدار VIF/Tolerance را در یک سلول می‌گذاشت،
     به دو جدول مستقل و خوانا تبدیل شد.
   - جدول **۶-۱ عدم هم‌خطی چندگانه** برای هر پیش‌بین یک ردیف مستقل دارد؛ سرستون دوطبقهٔ آن شامل
     متغیر وابسته، پیش‌بین، گروه «عدم هم‌خطی چندگانه»، تلورانس، VIF و نتیجه است. سلول متغیر وابسته
     در HTML با `rowSpan` گروه‌بندی می‌شود.
   - جدول **۶-۲ استقلال خطاها** برای هر معادلهٔ وابسته فقط یک ردیف شامل دوربین–واتسون، دامنهٔ
     قابل قبول ۱٫۵ تا ۲٫۵ و نتیجه دارد. دو شرط برقرار بودن نیز جداگانه نمایش داده می‌شوند.
   - `src/lib/sem-diagnostics.ts` منبع واحد ساخت ردیف‌ها برای UI، گزارش متنی و Word است؛ بنابراین
     ترتیب پیش‌بین و VIF در هر سه خروجی یکسان می‌ماند. نام‌های گزارش با حذف پسوند «(کل)» و نمایش
     نام خود زیرمقیاس تمیز شده‌اند.
   - محاسبهٔ VIF در `estimateSem` اصلاح شد: وقتی دادهٔ گمشده وجود دارد، همهٔ رگرسیون‌های کمکی VIF
     دقیقاً از complete cases همان معادلهٔ وابسته استفاده می‌کنند؛ این رفتار با جدول Coefficients
     در SPSS سازگار است. قبلاً حذف موردیِ جداگانه می‌توانست اختلاف جزئی بسازد.
   - مقدار دوربین–واتسونِ تعریف‌نشده دیگر به‌عنوان «برقرار» عبور نمی‌کند. معیار هم‌خطی نیز صریحاً
     `VIF < 5` و معادل آن `Tolerance > 0.20` است.
4. **شاخه:** `arena/01a05635-amarist`
5. **شماره کامیت کد:** `aac361dd21615d12f8a542dbe9f15f73f9f09649`
6. **نکته/راهنمایی برای ایجنت بعدی:**
   - ترتیب `sem.vifs[nodeId]` با ترتیب `sem.paths.filter(path => path.to === nodeId)` قرارداد دارد؛
     ردیف‌ها را با `buildSemRegressionDiagnostics` بسازید و دوباره جداگانه map نکنید.
   - تست با مقادیر SPSS کاربر دقیقاً این مقادیر را بازسازی کرد: Tolerance=`0.780, 0.673, 0.649,
     0.720`، VIF=`1.282, 1.485, 1.542, 1.388` و DW=`2.11`.
   - آزمون missing-data تأیید کرد VIF خروجی با VIF محاسبه‌شده روی complete cases مدل تا خطای
     کمتر از `1e-10` برابر است.
   - TypeScript، ESLint (صفر خطا؛ شش هشدار قدیمی)، Next production build (۹ مسیر استاتیک) و
     HTTP مسیر `/tools/sem` موفق بودند. هیچ دیتابیس یا بک‌اندی تغییر نکرد.


### نسخه ۱۰ — جهت واقعی اثر غیرمستقیم و آلفای ردیف‌به‌ردیف منطبق با SEM

1. **تاریخ و زمان انتشار (تهران، شمسی):** ۱۴۰۵/۰۶/۰۹ — ۱۰:۴۱ (تهران)
2. **آیا این آخرین نسخه است:** خیر (نسخهٔ ۱۱ آخرین است).
3. **توضیح مختصر تغییرات نسبت به قبلی:**
   - **جهت هر اثر غیرمستقیم یک قید واقعی شد.** هر ردیف جدول میانجی‌گری چهار حالت دارد:
     خودکار (پیش‌فرض)، مثبت، منفی و «مهم نیست». حالت خودکار از حاصل جهت مؤثر دو فلش مسیر—با
     لحاظ قیدهای صریح مسیر مستقیم—به دست می‌آید؛ اگر چند میانجی جهت‌های ناهمسو داشته باشند،
     جهت کل آزاد می‌شود. بررسی جهت داخل `evaluateAttempt` است و تلاش ناسازگار رد می‌شود، نه اینکه
     فقط یک برچسب نمایشی نشان داده شود.
   - **قرارداد فشردهٔ آلفا در `src/lib/sem-alpha.ts`.** هر `AlphaScale` شامل تعداد صریح کل گویه،
     فیلد عضویت هر زیرمقیاس (`1-5,7,9-12`)، حداقل/حداکثر مشترک و استثناهای انتخابی
     (`3=0-4; 7-9=1-7`) است. همهٔ زیرمقیاس‌ها و `hasTotal` همچنان از مشخصات متغیرها می‌آیند.
   - **اعتبارسنجی exact-once و مسدودکننده.** عبارت نامعتبر، شمارهٔ خارج از بازه، گویهٔ جاافتاده
     یا تخصیص تکراری با شماره‌های دقیق گزارش می‌شود. تولید، محاسبه، قالب اکسل و رفتن به مرحلهٔ بعد
     همگی ابتدا همان materialization سخت‌گیرانه را اجرا می‌کنند.
   - **تولید وابسته به دادهٔ SEM.** حجم نمونهٔ مستقل حذف شد؛ دادهٔ تولیدی/ایمپورت‌شدهٔ مرحلهٔ SEM
     اجباری است. در حالت دادهٔ واقعی، ستون‌ها از `colMap` خوانده می‌شوند. مقدار هر زیرمقیاسِ هر فرد
     فقط با انتقال‌های صفرجمع بین گویه‌های داخل دامنه توزیع می‌شود؛ بنابراین جمع دقیق حفظ می‌شود.
     در مقیاس جمع‌پذیر نیز جمع همهٔ گویه‌ها برابر جمع زیرمقیاس‌ها/نمرهٔ کل مدل است.
   - **عدم جعل موفقیت.** آلفای هر زیرمقیاس و کل مجاز باید هم‌زمان داخل بازهٔ انتخابی باشد. در
     ناسازگاری ریاضیِ دامنه، جمع هدف یا آلفا، هیچ داده‌ای تحویل نمی‌شود و پیام شامل واحد مسئله‌دار،
     ردیف‌های خارج از بازه یا نزدیک‌ترین آلفای یافت‌شده است.
   - **قانون ERQ حفظ شد.** برای `hasTotal: false` فقط زیرمقیاس‌ها ساخته و محاسبه می‌شوند؛ هیچ ردیف
     کل وجود ندارد. جریان مستقل ایمپورت اکسلِ دادهٔ واقعی آلفا نیز داخل همان بخش حفظ شده است.
   - **ابطال خروجی کهنه.** تغییر داده/نگاشت SEM، متغیرها، عضویت گویه‌ها یا دامنه‌ها خروجی آموزشی
     قبلی آلفا را پاک می‌کند تا نتیجه‌ای که دیگر با SEM برابر نیست صادر یا گزارش نشود.
   - **مهاجرت تنظیم قدیمی.** نگاشت item-by-item نسخهٔ ۹ فقط وقتی حفظ می‌شود که جمع دامنه‌هایش با
     دامنهٔ امتیاز مقیاس سازگار باشد؛ در غیر این صورت تعداد و دامنهٔ معتبر از مشخصات مقیاس استنتاج
     می‌شود (برای نمونه OGAI=۲۰، ERQ=۱۰، ناکامی=۱۶ و پرخاشگری=۲۹ گویه).
4. **شاخه:** `arena/01a05635-amarist`
5. **شماره کامیت کد:** `87538ce9ed81f30b4520f7c09d096188004ef31a`
6. **نکته/راهنمایی برای ایجنت بعدی:**
   - منبع حقیقت پیکربندی آلفا فیلدهای فشرده است؛ قبل از مصرف حتماً `materializeAlphaScales` را اجرا کنید.
   - تولید آموزشی آلفا عمداً مستقل نیست؛ `generateAlphaTrainingData` باید سری نمره‌های SEM را بگیرد.
   - آلفای کل فقط با `hasTotal: true` به `alphaUnits` اضافه می‌شود.
   - تست رفتاری جهت: خودکارِ مسیر مثبت×منفی، جهت منفی را پاس کرد؛ اجبار جهت مثبت همان مدل را پس از
     ۱۰۰ تلاش رد کرد. تست آلفا ۱۶ بررسیِ parser/coverage/bounds/sums/failure را پاس کرد و ۱۲۰ از
     ۱۲۰ جمع ردیفی دقیق بود (آلفاهای نمونه داخل ۰٫۷۰ تا ۰٫۹۰).
   - TypeScript، ESLint (صفر خطا؛ شش هشدار قدیمی)، build تولید Next (۹ مسیر استاتیک) و HTTP
     `/tools/sem`=200 موفق بودند. هیچ دیتابیس یا بک‌اندی تغییر نکرد.


### نسخه ۹ — تکمیل قانون واحد اثر، قیود β و همبستگی، و بازطراحی آلفا

1. **تاریخ و زمان انتشار (تهران، شمسی):** ۱۴۰۵/۰۶/۰۹ — ۰۸:۵۸ (تهران)
2. **آیا این آخرین نسخه است:** خیر (نسخهٔ ۱۰ آخرین است).
3. **توضیح مختصر تغییرات نسبت به قبلی:**
   - **قانون جهانی «واحد اثر» در جدول قیود کامل شد.** متغیرِ بدون نمرهٔ کل (مانند ERQ) دیگر
     در جدول مسیرهای مستقیم ردیف «همهٔ زیرمقیاس‌ها» و در جدول اثرهای غیرمستقیم ردیفِ سطح متغیر
     ندارد؛ فقط هر زیرمقیاس به‌صورت مستقل نمایش و سنجیده می‌شود. تابع `indirectUnitsOfVar` همین
     قانون را در هر سه جایگاه مبدأ، میانجی و مقصد اعمال می‌کند و `mlIndirectPoint` نیز می‌تواند
     هر سه جایگاه را به یک گره محدود کند.
   - **کلید پایدار برای قیود غیرمستقیم.** `indirectUnitKey` کلیدهای برچسب‌محور `p∷…` برای هر
     مسیر و `t∷…` برای کل اثر می‌سازد. این کلیدها بر کلیدهای عددی قدیمی مقدم‌اند، ولی کلیدهای
     قدیمی برای سازگاری با پروژه‌های ذخیره‌شده همچنان خوانده می‌شوند.
   - **بازهٔ الزامی و داورپسند β.** قدرمطلقِ β به‌طور پیش‌فرض باید بین `DEFAULT_BETA_MIN = 0.10`
     و `DEFAULT_BETA_MAX = 0.50` باشد. تفاوتِ `undefined` (هنوز تنظیم نشده؛ استفاده از پیش‌فرض)
     با `null` (کاربر فیلد را عمداً خالی کرده؛ بدون قید) در موتور و رابط حفظ شده است.
   - **همبستگیِ برون‌زاها تقویت شد.** کف همبستگی نهفته از ۰٫۱۵ به ۰٫۲۸ رسید، سقف ۰٫۴۵ ماند و
     `EXOG_MAX_VIF = 4` اضافه شد. `invertMatrix` و `maxVifOfCorrelation` همراه با چولسکی تضمین
     می‌کنند ماتریس مثبت‌معین و دور از هم‌خطی شدید باشد. گزینهٔ `enforceExogCorr` پیش‌فرضاً روشن
     است و در بخش ۵-۴ پیش‌فرض‌ها و هر دو گزارش متنی/Word نمایش داده می‌شود.
   - **حلقه‌های بازخورد مولد از هم جدا شدند.** `arrowScale` برای تفاوت هر فلش، `arrowBoost` برای
     معناداری، `arrowTrim` برای نگه‌داشتن β در بازه و `arrowMediation` برای کف اثر غیرمستقیم
     مستقل‌اند. تقویتِ میانجی هرگز کاهش نمی‌یابد و تعداد تلاش پیش‌فرض از ۲۰۰ به ۳۰۰ رسید.
   - **بخش آلفای کرونباخ داخل SEM بازطراحی شد.** زیرمقیاس‌ها و جمع‌پذیر بودن از بخش متغیرها
     خوانده می‌شود؛ هر گویه یک انتخاب‌گر زیرمقیاس دارد. آلفای هر زیرمقیاس همیشه جدا محاسبه می‌شود
     و آلفای نمرهٔ کل فقط برای پرسشنامه‌ای که `hasTotal: true` دارد ساخته می‌شود.
   - **دادهٔ تمرینی و واقعی آلفا جدا شدند.** دو تب مستقل، دانلود قالب، ایمپورت اکسلِ مستقل،
     اکسپورت اکسل و قرارداد نام ستون «متغیر — زیرمقیاس — گویهٔ N» اضافه شد. حجم نمونه از دادهٔ
     آلفا، سپس جدول مرحلهٔ داده و در نهایت n پروژه تشخیص داده می‌شود؛ فیلد خالی یعنی خودکار.
   - **تولید واقع‌گرایانهٔ دادهٔ آلفا.** زیرمقیاس‌ها از مؤلفهٔ مشترک و مؤلفهٔ اختصاصی با
     `rho` بین ۰٫۲۵ تا ۰٫۷۵ ساخته می‌شوند؛ توابع تصادفی بیرون از بدنهٔ کامپوننت‌اند تا خلوص
     React حفظ شود.
4. **شاخه:** `arena/01a05635-amarist`
5. **شماره کامیت کد:** `0155565d9fb49dfe8dd611fd954ca4d595aed0eb`
6. **نکته/راهنمایی برای ایجنت بعدی:**
   - `null` و `undefined` در قید β هم‌معنا نیستند؛ برای fallback از `??` استفاده نکنید.
   - `enforceExogCorr` یک پیش‌فرض علمیِ تولید است، نه شرط لازمِ عمومی SEM؛ کاربر می‌تواند آن را خاموش کند.
   - تابع‌های مولد آلفا را به بدنهٔ کامپوننت برنگردانید؛ ESLint React 19، `Math.random()` در مسیر
     render را با قانون `react-hooks/purity` خطا می‌گیرد.
   - ابزار مستقل `/tools/alpha` هنوز پیاده‌سازی قدیمیِ سطح متغیر را دارد؛ بازطراحی این نسخه مربوط
     به مرحلهٔ آلفای داخل `/tools/sem` است و انتقال آن به صفحهٔ مستقل یک تصمیم بازِ محصول است.
   - تست‌ها: TypeScript موفق؛ ESLint صفر خطا و شش هشدار قدیمی؛ build موفق با ۹ صفحهٔ استاتیک؛
     مسیرهای `/`، `/tools/sem` و `/tools/alpha` پاسخ ۲۰۰ دادند. مولد روی مدل ۴ سازه/۱۰ شاخص/n=۳۰۰
     در ۴ از ۴ اجرا همهٔ ۷۲ بررسی را پاس کرد (حدود ۱۳ تا ۲۲ ثانیه). تست آلفا در ۱۰ از ۱۰ اجرا
     برای همهٔ زیرمقیاس‌ها و نمره‌های کل موفق بود (چند میلی‌ثانیه).

### نسخه ۸ — اثر غیرمستقیمِ هر زیرمقیاس، قیدِ اختصاصیِ زیرمقیاس و شتابِ بوت‌استرپ

1. **تاریخ و زمان کامیت (تهران، شمسی):** ۱۴۰۵/۰۶/۰۹ — ۰۶:۲۴ (تهران)
2. **آیا این آخرین نسخه است:** خیر (نسخهٔ ۹ آخرین است).
3. **توضیح مختصر تغییرات نسبت به قبلی:**
   - **اثر غیرمستقیم برای متغیرهای غیرجمع‌پذیر تفکیک شد (مهم‌ترین تغییر؛ گزارشِ مستقیمِ کاربر).**
     «تنظیم هیجان» نمرهٔ کل ندارد، اما جدولِ اثرات غیرمستقیم یک ردیفِ واحد برای آن نشان می‌داد که
     حاصلِ جمعِ جبریِ دو زیرمقیاسِ ناهمسو بود: ارزیابی مجدد ۰٫۰۷۵− و فرونشانی ۰٫۰۸۴+ ⇒ خالص ۰٫۰۰۹
     با p≈۰٫۸۵ (یعنی «بی‌اثر»، در حالی که هر دو زیرمقیاس اثری کاملاً معنادار داشتند). اکنون
     «واحد اثر» برای متغیرِ جمع‌پذیر «کلِ متغیر» و برای متغیرِ غیرجمع‌پذیر «هر زیرمقیاس» است
     (`buildIndirectUnits`). ردیف‌ها فیلدهای `fromNode`/`viaNode`/`toNode` دارند و خروجیِ جدول،
     Word و گزارش متنی با `bootPathLabelOf` همان واحد را نشان می‌دهد. اثرِ مستقیم و کل نیز برای هر
     زیرمقیاس جداگانه جمع می‌شود.
   - **قیدِ اختصاصیِ هر زیرمقیاس (گزارشِ دومِ کاربر).** `GenConstraints` یک فیلدِ
     `nodePathTargets` گرفت که با کلیدِ **برچسبی** (`` `${fromLabel}→${toLabel}` ``، تابعِ
     `nodePathKey`) ذخیره می‌شود — نه شناسهٔ عددی، چون شمارهٔ گره‌ها با تغییرِ فهرستِ متغیرها عوض
     می‌شود و قیدِ کاربر را بی‌صدا خاموش می‌کند. اولویت در `resolvePathTargetForArrow`:
     زیرمقیاس ← متغیر ← استنتاج از قطبیتِ سازه. در رابط کاربر، برای هر جفتِ متغیرِ غیرجمع‌پذیر یک
     ردیفِ «همهٔ زیرمقیاس‌ها» (تنظیمِ یک‌جا) و به‌ازای هر زیرمقیاس یک ردیفِ مستقل نمایش داده
     می‌شود؛ ردیفِ زیرمقیاس مقادیر را از ردیفِ کلی ارث می‌برد تا وقتی که خودش تنظیم شود. نتیجه:
     می‌توان خواست که «ارزیابی مجددِ شناختی» اثری **منفی و معنادار** و «فرونشانیِ هیجانی» اثری
     **مثبت و معنادار** داشته باشد.
   - **بوت‌استرپ بیش از ۱۰ برابر سریع‌تر شد (گزارشِ سومِ کاربر).** دو تغییرِ مستقل: (الف)
     **گرادیانِ تحلیلیِ تابعِ هدفِ ML** در `sem-ml.ts` (`analyticGradient`): هر تکرارِ BFGS به‌جای
     ~۶۶ ارزیابیِ تابعِ هدف (تفاضلِ متناهی با p≈۳۰ پارامتر) به یک ارزیابی + یک گرادیان رسید؛
     (ب) **شروعِ گرم با وارونِ هسی‌ین** (`startInverseHessian`): تکرارهای لازم از ~۳۸ به ~۹ رسید.
     اندازه‌گیری روی یک نمونه‌گیریِ یکسان: ۳۵ms → **۶٫۵ms** به‌ازای هر نمونه، با همبستگیِ
     ۱٫۰۰۰۰ با اجرایِ کاملاً همگرا (آستانهٔ ۱e-۸). درستیِ گرادیان با مقایسه در برابر تفاضلِ مرکزی
     (خطایِ مطلق ≈ ۵e-۹) تأیید شده است.
   - **اجرای موازی در چند Worker.** `runBootstrap` یک استخر از Workerها به تعدادِ
     `navigator.hardwareConcurrency` (حداکثر ۸) می‌سازد، نمونه‌ها را بین آن‌ها تقسیم می‌کند و
     نمونه‌های خام را پیش از محاسبهٔ فاصلهٔ اطمینان ادغام می‌کند (`summarizeMlIndirect`). هر
     Worker یک «بذر» (پارامترها، ضرایب استاندارد، وارونِ هسی‌ین) می‌گیرد تا برآوردِ نمونهٔ کامل را
     تکرار نکند. ۲۰۰۰ نمونه: از ~۷۰ ثانیه به **۲ تا ۳ ثانیه**.
   - **تایمرِ بوت‌استرپ.** زمانِ سپری‌شده در حین اجرا (هر ۲۰۰ میلی‌ثانیه) و زمانِ کل پس از پایان،
     همراه با تعدادِ رشته‌های موازی و میانگینِ زمانِ هر نمونه نمایش داده می‌شود.
   - **نمونه‌های ناسالم کنار گذاشته می‌شوند.** اگر در یک نمونهٔ بازنمونه‌گیری‌شده هم‌خطیِ بسیار
     شدید باعثِ ضریبِ استانداردِ ناممکن شود (`|β| > ۱٫۵`)، آن نمونه از توزیع حذف می‌شود و تعدادِ
     نمونه‌های معتبر در زیرِ جدول گزارش می‌گردد (رفتاری مشابه AMOS با نمونه‌های ناهمگرا).
   - **تولید داده نیز سریع‌تر شد:** یک تولید کامل با ۲۰۰ تلاش و دو راستی‌آزمایی که قبلاً حدود
     ۶۰ ثانیه زمان می‌برد، اکنون حدود ۲۱ ثانیه است.
4. **شاخه:** `arena/01a050e8-amarist`
5. **شماره کامیت:** `330ee7cf040a9fc11aebfb7096e004163c407784`
6. **نکته برای ایجنت بعدی:**
   - «واحد اثر» یک قانونِ سراسری است: هر جا ردیفی از اثرات (مستقیم/غیرمستقیم/کل) ساخته می‌شود،
     ابتدا بپرسید «این متغیر نمرهٔ کل دارد یا نه؟». پاسخ را با `buildIndirectUnits` بسازید، نه با
     فهرستِ متغیرها.
   - اگر روزی تابعِ هدفِ ML تغییر کرد (پارامترسازیِ جدید، بارِ متقاطع، مدلِ چندگروهی)،
     `analyticGradient` را هم به‌روز کنید و دوباره با تفاضلِ مرکزی راستی‌آزمایی کنید. گرادیانِ
     غلط خطا نمی‌دهد — فقط بی‌صدا به نقطهٔ اشتباه همگرا می‌شود.
   - قیدهایِ سطحِ زیرمقیاس با کلیدِ برچسبی ذخیره می‌شوند؛ اگر ساختارِ برچسبِ گره‌ها عوض شد،
     مهاجرتِ کلیدها را فراموش نکنید.
   - استخرِ Workerها در `runBootstrap` است؛ هر مسیرِ جدیدِ بوت‌استرپ باید یا نمونه‌های خام برگرداند
     (برای ادغام) یا تعدادِ رشته‌ها را در `setBootTiming` درست گزارش کند.
   - عددهایِ مرجع (Node، n=۳۰۰، مدل ۴ سازه / ۱۰ شاخص): بوت‌استرپ ~۶٫۵ms به‌ازای هر نمونه،
     برآوردِ ML کامل با خطای معیار ~۸۰ms، تولید کامل ~۲۱ ثانیه.

### نسخه ۷ — اصلاحِ موتورِ تولید داده: معناداریِ مسیرها، شتابِ بوت‌استرپ، همبستگیِ برون‌زا و جهتِ رابطه

1. **تاریخ و زمان کامیت (تهران، شمسی):** ۱۴۰۵/۰۶/۰۸ — ۱۵:۱۶ (تهران)
2. **آیا این آخرین نسخه است:** بله.
3. **توضیح مختصر تغییرات نسبت به قبلی:**
   - **معناداریِ مسیرها واقعاً اِعمال شد (مهم‌ترین تغییر).** در نسخهٔ ۶، رابط کاربر پیش‌فرضِ هر مسیر را
     «معنی‌دار باشد» نشان می‌داد، اما موتور در `evaluateAttempt` با `if (!t) continue` قید را کلاً رد
     می‌کرد (چون `constraints.pathTargets` پیش‌فرضاً تهی است). اکنون قیدِ مؤثر با
     `resolvePathTargetForArrow` استنتاج و واقعاً سنجیده می‌شود. برای اینکه این سنجش (که نیازمند
     خطای معیار/هسین عددی است) هزینهٔ تولید را بالا نبرد، ارزیابی **دومرحله‌ای** شده است: ابتدا
     برازشِ ارزانِ بدون خطای معیار، و فقط برای نامزدهای پذیرفته‌شده برازشِ کامل.
   - **کم‌برآوردیِ ضرایب برطرف شد.** مولد برای متغیرهای غیرجمع‌پذیر (مانند ERQ که نمره کل ندارد)
     خطای اندازه‌گیری (`λ≈۰٫۷`) اعمال می‌کرد، در حالی که `compileModel` در `sem-ml.ts` گره‌های
     تک‌شاخص را «مشاهده‌شدهٔ بدون خطا» می‌بیند (`latent: columns.length > 1`). نتیجه این بود که
     β برآوردشده ≈ β هدف × λ می‌شد (هدف ۰٫۲۹ → واقعی ۰٫۱۶) و مسیرها غیرمعنادار از آب درمی‌آمدند.
     اکنون برای این گره‌ها `λ = 1` است و β واقعی به هدف بسیار نزدیک شده است (۰٫۳۸ → ۰٫۳۷).
   - **همبستگیِ پیش‌بین‌های برون‌زا دیگر صفر نیست.** نمراتِ نهفتهٔ برون‌زا مستقل تولید می‌شدند،
     بنابراین ماتریس همبستگیِ پیش‌فرض‌ها مصنوعی (همه صفر) بود و پیش‌فرضِ «خطی بودن» بی‌معنا
     می‌شد. اکنون با `buildExogenousCorrelation` یک ماتریس همبستگیِ واقع‌گرایانه ساخته می‌شود که
     **علامتش از قطبیتِ سازه‌ها** می‌آید: دو سازهٔ هم‌جنس مثبت و دو سازهٔ ناهم‌جنس منفی
     (مثلاً ارزیابی مجدد ↔ اعتیاد ≈ ۰٫۲۵− و فرونشانی ↔ اعتیاد ≈ ۰٫۲۵+).
   - **قیدِ «جهت رابطه» اضافه شد.** نوعِ `PathTarget` یک فیلدِ `dir` («pos»/«neg»/«any») گرفت و
     جدولِ قیودِ مسیرهای مستقیم یک ستونِ «جهت رابطه» دارد. جهت به‌طور خودکار از روی نامِ سازه‌ها
     با `valenceOfLabel`/`inferPathDirection` استنتاج می‌شود (فهرستِ کلیدواژه‌های سازه‌های سازگار و
     ناسازگار؛ کلیدواژه‌های منفی اول چک می‌شوند تا «فرونشانی هیجانی» زیرمجموعهٔ «تنظیم هیجان»
     اشتباه گرفته نشود). برای زیرمقیاس‌های یک پرسشنامهٔ غیرجمع‌پذیر که جهتِ متفاوت دارند،
     گزینهٔ «خودکار (بر اساس زیرمقیاس)» نمایش داده می‌شود و جهت برای **هر فلش** جدا تعیین می‌گردد.
   - **بوت‌استرپ ۲٫۴ برابر سریع‌تر شد.** بهینه‌سازِ BFGS یک آستانهٔ همگرایی (`tolerance`) گرفت و
     بوت‌استرپ با `BOOTSTRAP_MAX_ITERATIONS = ۱۵` (به‌جای ۴۵) و `BOOTSTRAP_TOLERANCE = ۱e-۴` اجرا
     می‌شود. اندازه‌گیری روی یک نمونه‌گیریِ یکسان: ۸۵ → ۳۵ میلی‌ثانیه به‌ازای هر نمونه، در حالی که
     پهنایِ فاصلهٔ اطمینان تنها ۳٪ کمتر شد. پیش‌فرضِ تعداد نمونه‌ها نیز از ۵۰۰۰ به ۲۰۰۰ کاهش یافت
     (۷۰ ثانیه به‌جای ~۷ دقیقه).
   - **مقدارِ کمی نابرازشیِ واقع‌گرایانه (`CROSS_LOADING = ۰٫۱۵`).** پس از اصلاحِ λ، داده دقیقاً
     طبقِ مدل تولید می‌شد و برازش «بیش از حد کامل» بود (CFI ۰٫۹۹۴ از سقفِ ۰٫۹۹ عبور می‌کرد). اکنون
     آخرین زیرمقیاسِ متغیرِ نتیجه یک بارِ متقاطعِ اندک از سازهٔ نخست می‌گیرد — همان «واریانسِ روش»
     یی که در دادهٔ واقعی هست — و شاخص‌ها در بازهٔ واقع‌گرایانه می‌افتند.
   - **اثر غیرمستقیم برای متغیرهای غیرجمع‌پذیر اصلاح شد.** جمعِ جبریِ زیرمقیاس‌ها وقتی جهت‌ها
     مخالف بود یکدیگر را خنثی می‌کرد (ارزیابی مجدد ۰٫۱۰− و فرونشانی ۰٫۱۸+ ⇒ خالص ۰٫۰۸ در برابر
     آستانهٔ ۰٫۱۰). اکنون بازه برای **هر زیرمقیاس جداگانه و روی قدرِ مطلق** سنجیده می‌شود و
     معناداریِ «خالص» برای متغیر غیرجمع‌پذیر نادیده گرفته می‌شود (چون خالص در آن حالت بی‌معناست).
   - **کفِ پیش‌فرضِ اثر غیرمستقیم از ۰٫۱۰ به ۰٫۰۶ تغییر یافت.** اثر غیرمستقیم حاصل‌ضربِ دو ضریب
     است (a×b)؛ وقتی نتیجه چند پیش‌بین دارد و R² در بازهٔ ۰٫۳ تا ۰٫۶ مهار می‌شود، هر ضریب حدود
     ۰٫۳ می‌شود و حاصل‌ضرب به‌طور طبیعی ۰٫۰۶ تا ۰٫۱۵ است. کفِ ۰٫۱۰ در چنین مدل‌هایی عملاً
     دست‌نیافتنی بود و تولید را بی‌دلیل ناموفق می‌کرد.
   - **مقیاس‌بندیِ ضرایب بر اساسِ واریانسِ تبیین‌شده.** با پیش‌بین‌های همبسته، حاصل‌جمعِ ضرایبِ
     خام می‌توانست R² را به بیش از ۰٫۹ برساند. اکنون واریانسِ ترکیبِ خطی **با در نظر گرفتنِ
     همبستگیِ پیش‌بین‌ها** محاسبه و در صورت نیاز با یک ضریبِ واحد مقیاس می‌شود (الگویِ نسبی و
     جهتِ ضرایب حفظ می‌شود). کوواریانسِ نمراتِ نهفته (`covLat`) نیز به‌صورت بازگشتی به‌روزرسانی
     می‌شود تا همهٔ واریانس‌ها برابرِ ۱ بمانند و ضرایب همان β استاندارد باشند.
4. **شاخه:** `arena/01a050e8-amarist`
5. **شماره کامیت:** `9e7de301b94c102418f80105253feff561043e3a`
6. **نکته‌های راهنما برای ایجنت بعدی:**
   - **امتیاز = کمینهٔ حاشیهٔ قیود** (`Math.min(...checks.map(c => c.margin))`). بنابراین هرچه
     قیدهای کمتری سنجیده شود، امتیاز بالاتر می‌رود و نامزدِ «ارزان» برنده می‌شود. برای همین
     `bestFull` (نامزدی که ارزیابیِ کامل شده) جدا نگه داشته می‌شود و گزارش از آن ساخته می‌شود؛
     اگر این را حذف کنید گزارشِ نهایی گروه «مسیر مستقیم» را نشان نخواهد داد.
   - تستِ عملکردیِ مولد: فایل‌های `src/lib/sem-generator.ts` و `default-project-3.ts` را با
     `tsc --module commonjs` به یک پوشهٔ موقت کامپایل و با Node اجرا کنید (نیازی به tsx نیست).
   - اگر خروجیِ تولید باز هم ناموفق شد، نخست نگاه کنید کدام گروه در مرز است: تجربه نشان داد
     همهٔ شکست‌ها در مرزِ آستانه‌ها رخ می‌دهند (مثلاً p = ۰٫۰۹۹۹ یا اثر = ۰٫۰۹۲).


### نسخه ۶ — بازطراحیِ لندینگ، دارکِ پیش‌فرض، بوت‌استرپِ خودکار و دامنهٔ علمیِ قیود

1. **تاریخ و زمان کامیت (تهران، شمسی):** ۱۴۰۵/۰۶/۰۸ — ۰۹:۳۱ (تهران)
2. **آیا این آخرین نسخه است:** بله.
3. **توضیح مختصر تغییرات نسبت به قبلی:**
   - **نام پروژه‌ها شماره‌گذاری شد (۳٫۱):** «پروژه پیش‌فرض (شیردل)» → **«پروژه پیش‌فرض ۲»** و
     «پروژه پیش‌فرض (تارا رضوانی)» → **«پروژه پیش‌فرض ۳»**؛ پروژهٔ عمومی هم **«پروژه پیش‌فرض ۱»** شد.
     چون نام در localStorage ذخیره می‌شود، یک **مهاجرتِ یک‌باره و غیرمخرب** اضافه شد
     (`migrateDefaultProjectNames`) که فقط نام‌هایِ دقیقاً منطبق را عوض می‌کند و به نام‌های سفارشیِ کاربر
     دست نمی‌زند. فایلِ متادیتای استاتیک هم از `tara-razvani.ts` به **`default-project-3.ts`** تغییر نام داد.
     نکتهٔ عمدی: **شناسه‌ها و seed-keyهای localStorage تغییر نکردند** (`default-tara-razvani-v1` و …) چون
     تغییرِ آن‌ها برای کاربرانِ فعلی پروژهٔ تکراری می‌ساخت.
   - **صفحهٔ فرود بازطراحی شد (۳٫۲):** عنوانِ «حالت‌های پژوهشی» و توضیحِ زیرش حذف و با تیترِ بزرگ
     **«دستیار هوشمند تحلیل آماری برای اساتید و دانشجویان»** جایگزین شد (۳٫۴ + ۳٫۹). کارت‌ها گرادیانی،
     دارای نوارِ رنگی، آیکونِ برجسته و دکمهٔ «ورود به ابزار» با گرادیان شدند و متن‌هایِ توضیحیِ اضافی
     (bullets) از همهٔ کارت‌ها حذف شد (۳٫۸).
   - **دارک پیش‌فرض شد (۳٫۲):** مؤلفهٔ جدید `src/components/theme-toggle.tsx` با
     `useSyncExternalStore` (بدون خطایِ هیدریشن و بدون setState در effect). یک اسکریپتِ پیش از هیدریشن در
     `src/app/layout.tsx` کلاسِ `dark` را اعمال می‌کند تا پرشِ تم نداشته باشیم. سوییچ در **هر دو هدر**
     (صفحهٔ فرود و ابزارها) در دسترس است.
   - **فوتر پاک‌سازی شد (۳٫۳):** «گیت‌هاب»، «ساخته‌شده با Next.js · آماده‌ی استقرار روی Vercel» و «© ۱۴۰۵»
     حذف شدند؛ فقط لوگو/شعار و دکمهٔ «دربارهٔ برنامه» مانده است.
   - **تیکِ «مرحله پیگیری» جابه‌جا شد (۳٫۵):** از کارتِ لندینگ حذف و به مرحلهٔ **«تشخیص»** در
     `src/components/clinical-tool.tsx` منتقل شد (فقط در حالتِ مقایسهٔ دو درمان). تغییرِ آن داده و تحلیلِ
     قبلی را باطل می‌کند، چون ساختارِ زمانی از ۲ به ۳ زمان عوض می‌شود.
   - **بوت‌استرپ خودکار + نوار پیشرفت (۳٫۶):** `src/workers/sem-bootstrap.worker.ts` حالا پیام‌هایِ
     `progress` می‌فرستد؛ `runBootstrap` یک آپشنِ `onProgress` می‌گیرد و state جدیدِ `bootProgress`
     درصدِ پیشرفت را نگه می‌دارد. بوت‌استرپِ اثرات مستقیم/غیرمستقیم/کل **بلافاصله پس از تولید داده یا
     اجرای تحلیل** اجرا می‌شود (هم دادهٔ تولیدی — تا با قیود تطبیق داده شود — و هم دادهٔ واقعی)؛
     دکمهٔ «اجرای بوت‌استرپ» برای نمونه‌گیریِ دوباره باقی مانده است. پارامترِ بلااستفادهٔ `boot` از
     `analyze` حذف شد.
   - **«دامنه علمی متداول» جلوی همهٔ قیود (۳٫۷):** ستون/خطِ سبز برای مسیرهای مستقیم (β)، اثرات غیرمستقیم،
     پیش‌فرض‌های آماری، بازهٔ R² (معيارِ کوهن) و تنظیماتِ عددی (حجم نمونه، تعداد نمونه‌های بوت‌استرپ،
     درصد گمشده، درصد پرت) اضافه شد.
   - رفعِ خطایِ قدیمیِ `prefer-const` در `clinical-tool.tsx` (تنها خطایِ eslintِ باقی‌ماندهٔ مخزن).
4. **شاخه:** `arena/01a050e8-amarist`
5. **شماره کامیت:** `f62148b0b6f2b0edd39a167770f8a804adf03aa0`
6. **اطلاعات اضافی برای ایجنت بعدی:**
   - `npm run build`، `npx tsc --noEmit` و `npx eslint src/` همگی **بدون خطا** هستند (فقط هشدارهایِ قدیمی باقی است).
   - Workerِ بوت‌استرپ با شبیه‌سازیِ `self` در Node تست شد: پیام‌هایِ پیشرفتِ ۵۰/۳۰۰ تا ۳۰۰/۳۰۰ و سپس
     نتیجهٔ نهایی دریافت شد (۳۰۰ نمونه ≈ ۲۴ ثانیه).
   - بوت‌استرپ ML ≈ ۸۵ms برای هر نمونه؛ با پیش‌فرضِ ۵۰۰۰ نمونه ≈ ۷ دقیقه طول می‌کشد — به همین دلیل حتماً
     باید در Worker بماند و نوار پیشرفت داشته باشد.
   - هیچ دیتابیس یا بک‌اندی تغییر نکرده است (ذخیره‌سازی فقط در localStorage مرورگر).

### نسخه ۵ — تولید چندتلاشه در SEM + مودال پیشرفت + گزارش قیود + دربارهٔ برنامه

1. **تاریخ و زمان کامیت (تهران، شمسی):** ۱۴۰۵/۰۶/۰۸ — ۰۸:۳۲ (تهران)
2. **آیا این آخرین نسخه است:** بله.
3. **توضیح مختصر تغییرات نسبت به قبلی:**
   - **رفع علتِ اصلیِ نقضِ قیود (مهم‌ترین تغییر):** موتور تولید قیود را با برآوردگرِ تقریبی (`approx`)
     می‌سنجید اما برنامه خروجی را با **ML** نشان می‌داد؛ تفاوتِ این دو بسیار زیاد است
     (در یک نمونهٔ واقعی: CFI تقریبی ۰٫۹۸۸ در برابر CFIِ ML برابر ۱٫۰۰۰، RMSEA تقریبی ۰٫۰۳۶ در برابر
     RMSEAِ ML برابر ۰٫۰۰۰، و R² تقریبی ۰٫۲۲/۰٫۳۶ در برابر R²ِ ML برابر ۰٫۳۴/۰٫۴۸). به همین دلیل قیودِ
     «واقع‌گرایانه» (سقفِ CFI/IFI و کفِ RMSEA) در خروجیِ نهایی نقض می‌شدند. حالا ارزیابیِ تولید
     دقیقاً با همان فراخوانی انجام می‌شود که برنامه در تحلیل نهایی به کار می‌برد
     (`estimateSem(..., measurementCols, "ml")`).
   - **`generateSemData` از همگام به ناهمگام (`async`) تبدیل شد** و یک شیءِ آپشن می‌گیرد:
     `{ maxAttempts, onProgress, shouldCancel, verifyBootSamples, maxVerifications }`.
     تابع دیگر در صورت شکست خطا پرتاب نمی‌کند؛ همیشه بهترین تلاش را همراه با «گزارش قیود» برمی‌گرداند.
   - **تعداد تلاش‌ها قابل تنظیم شد:** فیلدِ «تعداد تلاش‌های تولید» در مرحلهٔ «قیود تولید داده»
     (پیش‌فرض ۲۰۰؛ بازهٔ مجاز ۱ تا ۲۰۰۰). مقدار در `GenConstraints.maxAttempts` و در پروژهٔ
     localStorage ذخیره می‌شود و `normalizeConstraints` آن را برای پروژه‌های قدیمی مقداردهی می‌کند.
   - **مؤلفهٔ جدید `src/components/sem-generation-modal.tsx`:** هنگام تولید باز می‌شود و نشان می‌دهد
     در تلاشِ چندم هستیم، مرحلهٔ جاری چیست (تنظیم ضرایب / نمرات نهفته / ستون‌ها / برازش ML /
     بوت‌استرپ / ارزیابی / راستی‌آزمایی)، پیشرفتِ کلی و پیشرفتِ مرحله‌ای، و وضعیتِ زندهٔ هر گروه از قیود
     در بهترین دادهٔ ساخته‌شده. پس از پایان، همان مودال جدولِ گزارش را نشان می‌دهد.
   - **گزارشِ شفافِ قیود (`SemConstraintReport`):** جدولی با ستون‌های
     «گروه قید | قید | شرط لازم | مقدار به‌دست‌آمده | وضعیت» که گروه‌ها را جدا می‌کند
     (برازش مدل، مسیر مستقیم، اثر غیرمستقیم، R²، پیش‌فرض‌های آماری) و در انتها پیشنهادهای مشخص
     برای قیودِ نقض‌شده می‌دهد. خلاصهٔ همان گزارش در مرحلهٔ «تشخیص» با دکمهٔ «نمایش جدول قیود» هم در دسترس است.
   - **اجرا در Worker:** `src/workers/sem-generator.worker.ts` تولید را در نخِ جدا اجرا می‌کند؛
     صفحه قفل نمی‌شود، پیام‌های پیشرفت زنده می‌رسند و دکمهٔ «توقف تولید» کار می‌کند
     (در صورت نبودِ Worker، مسیرِ پشتیبان روی نخ اصلی با `await` بین تلاش‌ها اجرا می‌شود).
   - **راستی‌آزماییِ اثر غیرمستقیم با بوت‌استرپ ML:** غربالِ ارزان با بوت‌استرپ تقریبی (حداکثر ۳۰۰ نمونه)
     انجام می‌شود و نامزدِ پذیرفته‌شده با بوت‌استرپ ML (حداکثر ۳۰۰ نمونه) راستی‌آزمایی می‌شود؛
     پیشرفتِ نمونه‌به‌نمونه هم گزارش می‌شود (`onProgress` در `bootstrapSemMlIndirect` و
     `bootstrapIndirectEffects`). دامنهٔ اثر غیرمستقیم با نقطه‌برآوردِ ML سنجیده می‌شود (رایگان و دقیق).
   - **بهینه‌سازی:** وقتی کاربر قیدی روی مسیرها نگذاشته باشد، خطای معیار (هسین عددی) محاسبه نمی‌شود
     (`estimateSem(..., { standardErrors: false })`)؛ هر تلاش از ~۳۵۰ms به ~۱۹۰ms رسید.
   - **بخش «دربارهٔ برنامه»:** مؤلفهٔ `src/components/about-app.tsx` + فایلِ یکتای نسخه
     `src/data/app-version.ts` که نسخه، زمانِ انتشار (شمسی، تهران)، شاخه و شمارهٔ کامیت را نشان می‌دهد؛
     در هدرِ همهٔ ابزارها (`ToolHeader`) و فوترِ صفحهٔ فرود درج شده است.
   - **تلورانسِ عددی:** مقایسهٔ مرزِ قیود با epsilon انجام می‌شود تا خطای ممیز شناور
     (مثلاً R² = ۰٫۶۰۰۰۰۰۰۰۰۱ در برابر «حداکثر ۰٫۶») باعث مردودیِ اشتباه نشود.
4. **شاخه:** `arena/01a050e8-amarist`
5. **شماره کامیت:** `3fce2e6cc254ce83e7db0889ac83b6725b87b9c4`
6. **اطلاعات اضافی برای ایجنت بعدی:**
   - اندازه‌گیریِ واقعی با Node روی پروژهٔ پیش‌فرض «تارا رضوانی» (۴ متغیر، ۵ گره، ۱۰ ستون، n=۳۰۰،
     قیودِ پیش‌فرض): هر تلاش ~۰٫۲۲ ثانیه، میانگینِ تلاش تا موفقیت ~۶۵، نرخ موفقیت ۸/۸ با ۲۰۰ تلاش،
     زمانِ کل ~۳۰ تا ۶۰ ثانیه (که ~۲۵ ثانیه‌اش راستی‌آزمایی با بوت‌استرپ ML است).
   - هزینهٔ هر بخش (اندازه‌گیری‌شده): `estimateSem` تقریبی ≈ ۴ms، `estimateSem(...,"ml")` ≈ ۳۳۷ms،
     `estimateSemMl` بدون خطای معیار ≈ ۱۸۸ms، بوت‌استرپ تقریبی ۱۰۰۰ نمونه ≈ ۵۰۰ms،
     **بوت‌استرپ ML ≈ ۸۵ms به‌ازای هر نمونه** (یعنی ۵۰۰۰ نمونه ≈ ۷ دقیقه — برای همین در تولید سقف می‌خورَد).
   - `npm run build` و `npx tsc --noEmit` هر دو موفق‌اند؛ `eslint` روی فایل‌های تغییریافته بدون هشدار است
     (یک خطایِ قدیمیِ `prefer-const` در `src/components/clinical-tool.tsx:314` از قبل وجود داشته و دست نخورده است).
   - هیچ دیتابیس یا بک‌اندی تغییر نکرده است (ذخیره‌سازی فقط در localStorage مرورگر).

### نسخه ۴ — بازبینی بالینی + ابزار پیش‌بینی رگرسیونی + صفحهٔ فرود پنج‌کارتی

1. **تاریخ و زمان کامیت (تهران، شمسی):** ۱۴۰۵/۰۶/۰۷ — ۱۰:۰۱ (تهران)
2. **آیا این آخرین نسخه است:** بله.
3. **توضیح مختصر تغییرات نسبت به قبلی:**
   - بازگرداندن/افزودن بخش‌های حذف‌شده در ابزارهای بالینی طبق بازخورد کاربر:
     - «مشخصات طرح» حالا شامل **عنوان مداخله** و **نوع مداخله** (آموزشی/روان‌درمانی/دارویی/…) است.
     - بخش جداگانهٔ **متغیرها** (متغیر وابسته: نام، ابزار/مقیاس، سطح اندازه‌گیری، دامنه نمره).
     - بخش جداگانهٔ **سطح معناداری (آلفا)** با پیش‌تنظیم‌های ۰٫۰۵/۰٫۰۱/۰٫۰۰۱.
     - جدا شدن **یافته‌های توصیفی** از **یافته‌های استنباطی** (دو مرحلهٔ مجزا در استپر).
   - «مقایسهٔ اثربخشی دو درمان» به **سه گروه** (کنترل، درمان ۱، درمان ۲) برگشت (مطابق ساختار اولیه)؛
     «اثربخشی یک مداخله» دو گروه با نقش «گروه ۱ = کنترل، گروه ۲ = آزمایش/درمان».
   - نام ابزار اول از «اثربخشی یک درمان» به **«اثربخشی یک مداخله»** تغییر کرد.
   - صفحهٔ فرود: حذف دو توضیح دسته‌ها، چیدن **پنج کارت کنار هم**، بولت‌پوینت به‌جای توضیح بلند،
     دکمهٔ «ورود به ابزار» به‌جای بج «ابزار آماده»، و چک‌باکس «مرحله پیگیری» روی کارت مقایسه (کوئری `?followup=1|0`).
   - ساخت ابزار جدید **«پیش‌بینی رگرسیونی»** (`/tools/regression`): رگرسیون خطی چندگانه با
     تولید دادهٔ تمرینی (R² هدف)، ضرایب B و β استاندارد، R² و R² تعدیل‌شده، F، ماتریس همبستگی،
     گزارش متن/docx و پشتیبان‌گیری. فایل‌های جدید: `src/lib/regression.ts`، `src/lib/regression-report.ts`،
     `src/components/regression-tool.tsx`، `src/app/tools/regression/*`.
   - تعمیم کتابخانه‌ها به تعداد گروه دلخواه: `groupedByGroup` و `rowsToGrouped` در `clinical-stats.ts`
     و موتور تولید در `clinical-generator.ts` (حالا `nGroups` و `nTimes` می‌گیرد).
4. **شاخه:** `arena/01a04632-amarist`
5. **شماره کامیت:** `3dc0d802055d47d6aa016f9b7949a741b6546269`
6. **اطلاعات اضافی برای ایجنت بعدی:**
   - `npm run build` و `tsc --noEmit` هر دو موفق‌اند (۹ صفحهٔ استاتیک، شامل `/tools/regression`).
   - موتور تولید با Node صحت‌سنجی شد: طرح کنترل (d هدف)، پیگیری سه‌گروهی ×۳ زمان (η² هدف) و
     سه‌گروهی ×۲ زمان، و رگرسیون (R² هدف + معنی‌داری همه ضرایب) همگی همگرا شدند؛ سه‌گروهیِ
     پیگیری در ۳۰/۳۰ اجرا موفق بود (میانگین ~۳ تلاش).
   - هیچ دیتابیس یا بک‌اندی تغییر نکرده است (ذخیره‌سازی فقط در localStorage مرورگر).

### نسخه ۳ — بازسازی ابزارهای بالینی (اثربخشی یک درمان / مقایسهٔ دو درمان)

1. **تاریخ و زمان کامیت (تهران، شمسی):** ۱۴۰۵/۰۶/۰۷ — ۰۷:۰۵ (تهران)
2. **آیا این آخرین نسخه است:** خیر (نسخهٔ ۴ آخرین است).
3. **توضیح مختصر تغییرات نسبت به قبلی:**
   - «اثربخشی یک درمان» و «مقایسهٔ اثربخشی دو درمان» با یک مؤلفهٔ مشترک بازسازی شدند تا
     تجربهٔ کاربری و کامل بودنِ ابزار SEM را داشته باشند: استپر چندمرحله‌ای، پروژه‌های
     غیرمخرب در localStorage، تولید دادهٔ تمرینی هدفمند، تحلیل دادهٔ واقعی، جدول دادهٔ
     کاملاً قابل ویرایش، بررسی پیش‌فرض‌ها، یافته‌ها، گزارش کامل (متن/docx/TSV) و پشتیبان‌گیری/بازیابی.
   - طرح «اثربخشی یک درمان» (`mode="one"`): گروه مداخله در برابر کنترل، پیش/پس؛ تحلیل با
     t مستقل روی نمرهٔ تغییر + ANCOVA + d کوهن.
   - طرح «مقایسهٔ دو درمان» (`mode="compare"`): دو گروه مستقل × پیش/پس/پیگیری؛ آنوا آمیخته
     (گروه/زمان/تعامل) + بن‌فرونی درون‌گروهی + پیش‌فرض‌ها (نرمالیتی/لون/Box's M/موخلی).
   - هر ابزار در اولین باز شدن یک پروژهٔ نمونهٔ پیش‌فرض در localStorage می‌سازد.
   - فایل‌های جدید: `src/components/clinical-tool.tsx`، `src/components/clinical-stepper-shell.tsx`،
     `src/lib/clinical-stats.ts`، `src/lib/clinical-generator.ts`، `src/lib/clinical-report.ts`.
   - پیاده‌سازی قدیمی `compare-treatments` جایگزین شد (بازطراحی، نه صرفاً تغییر ظاهر)؛
     صفحهٔ جدید `/tools/one-treatment` اضافه شد و `research-modes.tsx` به آن لینک می‌دهد.
   - حذف `src/components/follow-up-toggle.tsx` (مؤلفهٔ قدیمی بدون مصرف).
4. **شاخه:** `arena/01a04632-amarist`
5. **شماره کامیت:** `13c46ce52e2c451b6a1e5fd14fec66d49755dd60`
6. **اطلاعات اضافی برای ایجنت بعدی:**
   - `npm run build` و (بعد از اجرای dev/build) `tsc --noEmit` هر دو موفق‌اند.
   - گزارش‌های بالینی با اجرای مستقیم Node روی دادهٔ تولیدی هر دو طرح (control و followup)
     صحت‌سنجی شدند (خروجی متن و بایت‌های docx بررسی شد).
   - هیچ دیتابیس یا بک‌اندی تغییر نکرده است (ذخیره‌سازی فقط در localStorage مرورگر).

### نسخه ۲ — پروژهٔ پیش‌فرض «تارا رضوانی» در ابزار SEM + ادغام دو خط کاری

1. **تاریخ و زمان کامیت (تهران، شمسی):** ۱۴۰۵/۰۶/۰۶ (تهران)
2. **آیا این آخرین نسخه است:** بله.
3. **توضیح مختصر تغییرات نسبت به قبلی:**
   - پایهٔ این نسخه، آخرین کامیت ابزار SEM است: `8bf7abe` از شاخهٔ `arena/01a00a94-amarist`
     (کاربر آن را «آخرین و بی‌نقص» معرفی کرد).
   - افزودن `taraRazvaniVars` و منطق seeding در `src/app/tools/sem/page.tsx`: پروژهٔ پیش‌فرض
     «پروژه پیش‌فرض (تارا رضوانی)» با ۴ متغیر SEM (اعتیاد به بازی‌های آنلاین = برون‌زا،
     تنظیم هیجان = برون‌زا بدون نمرهٔ کل، احساس ناکامی = میانجی، پرخاشگری = درون‌زا) و
     دامنه‌های دقیقِ تأییدشده از متن فصل سوم.
   - بازآرایی seeding به دو تابع `ensureShirdelProject` و `ensureTaraRazvaniProject`
     (ادغام‌پذیر و غیرمخرب برای پروژه‌های موجودِ کاربر).
   - ادغام فایل‌های شاخهٔ `arena/01a037d0-amarist`: `src/data/default-projects/*` (متادیتای
     استاتیک پرسشنامه‌ها) + `AGENT_GUIDE.md` + `VERSION_LOG.md`.
4. **شاخه:** `arena/01a04632-amarist`
5. **شماره کامیت:** `73f659769357af6efc50cd3a12eb0be209aa7b1a`
6. **اطلاعات اضافی برای ایجنت بعدی:**
   - `LayoutProps` در `src/app/layout.tsx` خطا نیست؛ API معتبر Next.js 16 است و تایپش توسط
     Next در `.next/types` تولید می‌شود. `npm run build` و (بعد از اجرای dev/build) `tsc`
     هر دو موفق‌اند (در این نسخه تأیید شد).
   - ترتیب seed شدن پروژه‌ها در localStorage: «پروژه پیش‌فرض» → «شیردل» → «تارا رضوانی».
   - دو مفهوم «پروژهٔ پیش‌فرض» وجود دارد؛ توضیح کامل در `AGENT_GUIDE.md` بخش ۳.
   - هیچ دیتابیس یا بک‌اندی تغییر نکرده است (ذخیره‌سازی فقط در localStorage مرورگر).

### نسخه ۱ — ایجاد پروژه‌های پیش‌فرض (استاتیک) + پروژه «تارا رضوانی»

1. **تاریخ و زمان کامیت (تهران، شمسی):** ۱۴۰۵/۰۶/۰۳ — 11:06 (تهران)
2. **آیا این آخرین نسخه است:** خیر (نسخهٔ ۲ آخرین است).
3. **توضیح مختصر تغییرات نسبت به قبلی:**
   - نخستین نسخهٔ این فایل مدیریت نسخه‌ها بود (قبلاً وجود نداشت).
   - ایجاد قرارداد داده‌ای پروژه‌های پیش‌فرض در `src/data/default-projects/types.ts`.
   - افزودن نخستین پروژه پیش‌فرض استاتیک «تارا رضوانی» (`src/data/default-projects/tara-razvani.ts`)
     شامل ۴ پرسشنامه: OGAI، ERQ، AQ، مقیاس احساس ناکامی + پرسشنامه جمعیت‌شناختی.
   - ثبت فهرست پروژه‌ها در `src/data/default-projects/index.ts`.
   - ایجاد `AGENT_GUIDE.md` (راهنمای ایجنت بعدی).
4. **شاخه:** `arena/01a037d0-amarist`
5. **شماره کامیت:** `a60be21965385114f9ab31ff188683fc16a9ce36`
6. **اطلاعات اضافی برای ایجنت بعدی:**
   - این نسخه روی شاخهٔ موازی `arena/01a037d0-amarist` (بدون ابزار SEM) ساخته شد و در نسخهٔ ۲
     به خط اصلی ادغام شد.
   - هشدارِ قدیمی دربارهٔ `LayoutProps` در `src/app/layout.tsx`: بعداً در نسخهٔ ۲ مشخص شد که
     خطا نیست و فقط به تولیدِ تایپ‌های Next (`.next/types`) نیاز دارد.
   - ترتیب گویه‌ها در خرده‌مقیاس‌های OGAI و AQ بر اساس ساختار متداول منتشرشده درج شده و در
     یادداشت (`note`) هر خرده‌مقیاس علامت‌گذاری شده که با متن دقیق نسخه فارسی بازبینی شود.
   - هیچ دیتابیس یا بک‌اندی در پروژه وجود ندارد (ذخیره‌سازی در localStorage).
